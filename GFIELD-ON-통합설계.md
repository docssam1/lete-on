# GFIELD-ON 통합 플랫폼 — 설계 · 운영 인수인계서 (v1.0)

> 성격: `lete-on` 저장소를 **하나의 통합 플랫폼**으로 묶는 설계 기준 문서.
> 원칙: **통합관리 한 곳이 유일 출처 → 각 인덱스는 읽어서 뿌리기만** 한다.
> 상태: v1.0 (2026-07 · 통합관리 + config.js 뼈대 반영). 다음은 각 인덱스 개조.

---

## 0. 한 줄 요약

> 원장님이 **통합관리(admin) 한 곳**에서 구조·프로그램·회원권한을 관리하고 [저장]하면 →
> **`config.js` 한 파일**이 갱신되고 → **모든 인덱스(대문·필즈·하이퍼포커스·로드맵·황소…)** 가 그 파일을 읽어 화면을 그린다.
> 학생은 **승인번호 하나**로 로그인하면, 산 것 전부가 어디서든 열린다. (서버 호출 없이 즉시)

---

## 1. 핵심 구조

### 1-1. 유일 출처 = `config.js`

- **바꾸는 곳은 통합관리 한 곳뿐.** 각 폴더(인덱스)는 개조 후 손대지 않는다.
- 폴더를 직접 여는 건 **디자인·동작(기능)을 바꿀 때만** (드묾, 개발 작업).

### 1-2. 왜 정적 파일인가 (속도)

- Apps Script를 읽기 경로에서 쓰면 매번 300ms~1초 지연(콜드스타트). 그래서 **읽기 경로엔 서버를 두지 않는다.**
- 구조·콘텐츠는 원래 정적. 느린 회원·권한 조회마저 **해시된 정적 맵**으로 만들어 `config.js`에 넣으면 → 서버 호출 0, **CDN 즉시 로딩**.
- 필즈가 이미 쓰던 SHA-256 해시 방식을 중앙화한 것.

---

## 2. 대비 과정 구조 (3축)

```
소마             →  소마 대비 · 소마 프리미어 대비(하이퍼포커스 · 온라인 모의고사 1~8회)
필즈 더 클래식    →  개념 · 킬러 · 파이널 · 진단 · 자료실   (과정 하나 → 바로 서재)
생각하는 황소     →  초등 대비 · 중등 대비
```

- 축마다 깊이가 다르다: 소마·황소는 [축 → 과정 → 서재] 3단, 필즈는 [축 → 서재] 2단.
- 모든 모의고사는 **[영상 + 교재]** 한 세트 뷰어로 통일.

---

## 3. 통합 로그인 (승인번호 = 회원번호식)

- **회원 1명 = 승인번호 1개.** 뭘 사든 그 번호에 권한이 **누적**. 로그인 한 번으로 산 것 전부 열림.
- 로그인 흐름(빠름): 승인번호 입력 → 브라우저에서 SHA-256(salt+번호) 해시 → config.js의 access 맵 조회 → 권한 열쇠 획득 → 자물쇠 해제 (네트워크 왕복 0).
- **원문 승인번호·이름은 config.js에 없다.** 통합관리 원장 레지스트리(브라우저 저장 + 내보내기 백업)에만 있고, 공개 파일엔 **해시만**.

### 3-1. 권한 열쇠 규칙

| 단위 | 열쇠 예시 |
|---|---|
| 축(브랜드) | `soma` · `fields` · `hwangso` |
| 과정 | `soma:premier` · `hwangso:elem` |
| 프로그램 | `fields:concept` · `soma:premier:hyperfocus` |
| 모의고사 회차 | `soma:premier:mock:1` ~ `mock:8` (회차별 개별 개방) |

---

## 4. `config.js` 데이터 구조

> 반드시 `window.GFIELD_CONFIG = {` 로 시작, `};` 로 끝. 통합관리가 자동 생성.

```
meta      : { updatedAt, salt, adminPass? }
brands[]  : { key, name, icon, badge, desc, single?, courses[]?, programs[]? }
program   : { key, name, type, desc, lock, url, count? }
              // lock: "open"(미리보기) | "closed"(승인필요) | "soon"(준비중)
access    : { "<SHA-256(salt+승인번호)>": ["권한열쇠", ...] }   // 해시만
```

---

## 5. 통합관리(admin) 사용법

파일: `gfield-on-admin.html` · 비밀번호: `01020837265` (config `meta.adminPass`로 변경 가능)

| 탭 | 하는 일 |
|---|---|
| ① 구조·프로그램 | 축/과정/프로그램 추가·수정·삭제. 잠금·링크·회차 설정 |
| ② 회원·권한 | 회원 발급(승인번호 자동생성) → 권한 열쇠 체크. 레지스트리 내보내기/불러오기 |

- 끝나면 **[💾 GitHub 저장]** → 회원권한 해시 → `access` 생성 → `config.js` 커밋 → 1~2분 후 반영.
- 저장엔 **GitHub PAT**(Contents: Read/Write) 최초 1회.
- **원장 레지스트리(이름·원문 코드)는 이 브라우저에만.** 기기 옮길 땐 내보내기/불러오기.

---

## 6. 각 인덱스 개조 방법 (하나씩)

1. 상단에 `<script src="/config.js"></script>` 추가.
2. 하드코딩 데이터(필즈 `PRODUCTS`, 하이퍼포커스 `777200` 등) 삭제 → `window.GFIELD_CONFIG`에서 읽기.
3. 로그인: 승인번호 → 해시 → `GFIELD_CONFIG.access[해시]`로 권한 확인.
4. 개조 끝난 폴더는 이후 손대지 않는다.

권장 순서: **필즈 → 하이퍼포커스 → 대문/허브 → 로드맵 → 황소.**

---

## 7. 보안 · 트레이드오프

- 회원 맵은 **해시 상태**로 공개 → 원문 코드 노출 없음. 단 **긴 랜덤 코드 + salt** 필수.
- 권한 변경 반영 **1~2분**(배포 지연). 즉시 차단은 그만큼 못 함.
- 즉시성·비밀유지 필요한 것(진단 저장 등)만 Apps Script **쓰기 전용**으로.

---

## 8. 파일 지도

```
lete-on/
├─ index.html            ← 대문(소개) · 예정
├─ config.js             ← ★유일 출처
├─ gfield-on-admin.html  ← 통합관리 콘솔
├─ hyper-focus/          ← 하이퍼포커스 진단 (개조 대상)
├─ fields-classic/       ← 필즈 (개조 대상)
├─ roadmap/              ← 로드맵 (독립)
└─ docssam_chatbot/      ← 상담 엔진 자료
```

---

## 9. 다음 단계

```
[1] 통합관리 + config.js 실배포 ✅
[2] 필즈 폴더 개조 — config.js 읽기 + 공통 로그인
[3] 대문(소개 시안) + 허브를 config.js 읽도록 연결
[4] 하이퍼포커스·로드맵·황소 순차 개조
[5] 비어있는 프로그램 채우기 (소마 대비 · 황소 초등·중등)
```

---
*최종 업데이트: 2026-07 · 작성 보조: Claude (Cowork)*
