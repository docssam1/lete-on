# 개념 애니메이션 설계 — 창의수연 85유닛 + 중고등 62유닛 (2026-08-29)

> 원장 지시: "난 우리 개념에도 필요할 것 같아 — 창의수연이나 중고등 뜨는 고학년 연산에."
> 대상 스킬: `skills/gmap-animated-math-lesson` (GFIELD 자체 제작). **포크하지 않고 확장한다.**

---

## 1. 왜 — 지금 개념 화면의 실제 상태

**유닛 187개 중 186개가 `mathSteps`, 즉 문자열 배열이다. 그림을 그리는 유닛은 1개뿐이다.**

A-05(100의 보수)의 마법 노트:

```js
mathSteps:['25 + 75 = 100  (짝!)', '40 + 60 = 100  (짝!)', '63 + 37 = 100  (짝!)']
```

그런데 이 전략의 본질은 `25+75+54+46` → `(25+75)+(54+46)`로 **짝을 찾아 묶는 동작**이다.
그 움직임이 곧 마법인데 텍스트 세 줄로는 아이가 머릿속에서 상상해야 한다.

| 구간 | 유닛 | 왜 급한가 |
|---|---|---|
| 창의수연 A·C·H | **85** | 전략 자체가 "펼치고·옮기고·묶는" 동작. 정적 텍스트와 가장 안 맞는다 |
| 중고등 M | **62** | `과정-로드맵.md` §13이 "기호가 바뀌는 순간이 진짜 고비"라 못 박고 **말→그림→내 표기→표준 기호** 순서를 규정했는데, **정작 '그림' 단계가 화면에 없다** — 원칙이 문서에만 있는 상태 |

## 2. 원칙

1. **스킬을 포크하지 않는다.** `skills/gmap-animated-math-lesson`에 archetype을 **보태서** 두 앱
   (boarding-school-math, number_magic)이 같은 스킬을 쓴다. 도형 3종(geometry angle·shape count·
   coordinate plane)은 넘버스와 무관하고, 넘버스가 필요한 **수 펼치기 계열이 스킬에 없다** — 그걸 채운다.
2. **장면을 손으로 그리지 않는다.** 생성기 반환값이 곧 장면 객체다(§4).
3. **`mathSteps` 186개를 버리지 않는다.** 각 줄에 `targetIds`를 달아 그림과 잇는다 — 승격이지 폐기가 아니다.
4. **자기보고를 근거로 삼지 않는다.** 이 저장소는 "맞다고 판단한 뒤" 검사기가 12건을 잡은 전례가 있다
   (HANDOFF). 85개를 눈으로 다 볼 수 없으므로 §6 검사기가 먼저 있어야 한다.

## 3. 동작 계열 8종 (신규 archetype)

창의수연 78개 생성기의 반환 필드를 전수 조사해 묶은 것이다. 이름은 아이가 쓰는 말을 따른다.

| # | 계열 | 무엇이 움직이나 | 해당 생성기(예) |
|---|---|---|---|
| 1 | **묶기** group | `nums[]`에서 조건에 맞는 짝·그룹이 묶여 부분합으로 접힘 | comp100, pair10, pair10_2d, addSubGroup(2), splitPlace, jumpAdd, stairAdd, adv_gather, adv_comp100, ml_pair10 |
| 2 | **이사** transfer | `amount`가 한 수에서 다른 수로 옮겨가 라운드수를 만들고 나머지가 남음 | move10, move10_2d, add10sub, deficientFrom10, galaxy999, adv_1001, ml_end9, adv_anchorTens |
| 3 | **쪼개기** split | 한 수가 자릿값 단위로 갈라져 각각 계산된 뒤 다시 합쳐짐 | splitHundred, splitAdd2Digit, splitAddByDigit, splitSubtract, splitTwo, digitShiftSub, subByPlace, ml_div_decomp, ml_partial |
| 4 | **보정** compensate | 라운드수로 바꿔 계산하고 **넘치거나 모자란 만큼을 되돌림** | addSimilarNums, addSameSub, subSameSub, moveAndSub, subEasyFirst, expandRewrite, ml_overmul, adv_estimate, adv_divNear |
| 5 | **자리 이동** place-shift | 자릿수가 통째로 이동하고 그만큼 보정 연산이 붙음(×5=×10÷2) | ml_x5, ml_x25, ml_x11, ml_x9, ml_placeshift, ml_decimal_mul, ml_decimal_div, adv_bigscale |
| 6 | **부분곱** partial-product | 곱셈이 부분곱으로 분해되어 격자·교차로 누적됨 | ml8_mul2d2d, ml9_mul3d2d, ml6_mul2d1dMental, ml_veda, ml11_squares, adv_nearSquare, adv_splitSquare, ml_diff2sq, adv_sqcube |
| 7 | **무지개** pair-sum | 양 끝에서 짝을 지어 같은 합이 반복됨(첫+끝)×쌍수 | ml_gauss, gaussAdd1, adv_sumSquares, countBetween |
| 8 | **표기 바꾸기** notation | 같은 값의 **두 표기를 나란히** 두고 대응을 보임 | romanNumerals, ml_frac_conv, fr8_frDec, adv_baseSystem, adv_repeatDec, ml_frac_* |

> 계열 8은 §13(기호 도감)의 "같은 것의 두 표기는 반드시 나란히"와 같은 규칙이다 —
> **중고등 62유닛의 기본 계열**이 되므로 여기서 함께 정의한다.

## 4. 데이터 계약 — 장면은 생성기에서 나온다

조사 결과 생성기가 **움직이는 대상을 이미 이름 붙여 내보내고 있다.** 손으로 그릴 필요가 없다.

```
comp100        → {nums, sum, pairCount, target:100}   묶기: nums 중 합이 target인 짝
move10         → {a, b, need, rest}                   이사: need를 b→a, a는 10, b엔 rest
splitHundred   → {a, b, rest, comp}                   쪼개기: 100 한 덩이를 떼어냄
digitShiftSub  → {a, b, before, comp}                 쪼개기/이사
addSubGroup    → {nums, plusSum, minusSum}            묶기: 부호별
addSimilarNums → {a, b, ra, rb, adj}                  보정: 라운드 ra·rb로 계산 후 adj 되돌림
gaussAdd1      → {n, pairSum, pairCount}              무지개
```

유닛당 작업은 **"이 생성기의 어느 필드가 이 archetype의 무엇인가"라는 10줄 안팎의 선언적 매핑**이지
장면 하나를 새로 디자인하는 게 아니다.

### 4-1. ⚠️ 두 생성기 계열의 비대칭 — 이것이 이 작업의 유일한 함정

| | 개수 | 내보내는 것 |
|---|---|---|
| `NM_GEN` (engine/generators.js) | 38 | **이름 붙은 의미 필드** (`need`·`rest`·`comp`·`plusSum`…) |
| `NM_TGEN` (threads/ml.js·adv.js) | 39 | **`steps:[{tex, blank}]`뿐 — 의미 필드가 하나도 없다** |

`ml_x9`가 그 예다. `n`·`n*10`·`answer`를 내부에서 전부 계산하지만 **`n`은 tex 문자열 안에만 있고
밖으로 나오지 않는다.** 이 상태로는 "×10 하고 n을 뺀다"를 그릴 수 없다.

**해법: tex를 파싱하지 말고, 생성기에 `scene:{...}`를 덧붙인다.**
이미 계산해 둔 지역변수를 내보내기만 하면 되므로 생성기당 2~3줄이고 **기존 반환값은 건드리지 않는다**
(문항·정답·인쇄 전부 불변). tex 파싱은 표기가 바뀌는 순간 조용히 깨지므로 금지한다.

```js
/* ml_x9 — 기존 반환에 scene만 추가. answer·steps·tex는 그대로. */
return {
  prompt, tex, answer, answerType:'steps', widget:'steps',
  steps:[…],
  scene:{ archetype:'place-shift', n, shifted:n*10, back:n, result:answer }
};
```

### 4-2. beat 계약

`mathSteps`(또는 `steps`)의 각 줄이 beat 하나가 되고, 각 beat는 자기가 가리키는 객체 id를 명시한다.
G·MAP `scene-contract.md`의 규칙(`draw`·`reveal`·`move`·`highlight`·`count`·`transform`)을 그대로 쓴다.

```
beat 1  highlight  targetIds:[num.25, num.75]   "더해서 100이 되는 짝을 찾아요"
beat 2  move       targetIds:[num.75]           25 옆으로 이동
beat 3  transform  targetIds:[group.1]          두 수가 100 한 덩이로 접힘
```

## 5. 구현 순서

| 단계 | 무엇 | 누가 |
|---|---|---|
| 1 | archetype 8종 정의 + `scene` 필드 계약 + 렌더러 + 검사기 | **Opus/Fable** — 여기서 틀리면 85개에 번진다 |
| 2 | 대표 3개로 실제 화면 확인 — **A-05**(묶기) · **C-06 ×9**(자리 이동) · **M-11**(표기 바꾸기) | Opus/Fable, 원장 확인 |
| 3 | `NM_TGEN` 39개에 `scene` 필드 추가 | Sonnet (기계적·검사기가 잡음) |
| 4 | 창의수연 85유닛 매핑 — 계열별 배치 | Sonnet |
| 5 | 중고등 62유닛 (계열 8 중심) | Sonnet |
| 6 | 틀린 문제 해설로 확장 — `steps` 보유 89스레드 | 이후 |

`HANDOFF.md`의 역할 분담(설계·검토 = Opus/Fable, 구현·반복 = Sonnet) 그대로다.

## 6. 검사기 요건 (1단계에 반드시 포함)

`skills/gmap-animated-math-lesson/scripts/validate_scene_manifest.py`를 확장한다.
**기계가 잡아야 하는 것:**

1. 모든 beat의 `targetIds`가 실재하는 객체를 가리키는가
2. 장면 객체의 수치가 생성기 반환값과 **일치**하는가 (그림이 문제와 다른 수를 그리면 잡는다)
3. 각 계열의 보존식이 성립하는가 — 묶기: 부분합의 합 = 전체합 / 이사: `a+b` 불변 /
   쪼개기: 조각의 합 = 원래 수 / 보정: 되돌린 뒤 정답 일치
4. beat 순서가 전체재생·단계학습에서 동일한가
5. 정답 객체가 답 beat 이전에 드러나지 않는가
6. 3개 언어 필드 존재 + 언어 혼입 없음

**시드 여러 개로 돌린다** — 생성기는 매번 다른 수를 내므로 한 번 맞은 것은 근거가 못 된다.

## 6-1. 렌더러는 새로 만들지 않는다 — Geometry 것을 재사용한다 (원장 지시, 2026-08-30)

> "기존 Geometry SVG 렌더러를 먼저 조사하고 재사용해. 좌표를 눈대중으로 그리지 말고
> 점·선분 모델에서 계산해 SVG를 생성해."

**대상: `geometry/worksheet/render.js`** (517줄, `GW_RENDER`). 조사해 보니 이미 우리가 필요한
방식 그대로다 — 새로 설계할 이유가 없다:

- **DOM 없는 문자열 빌더.** 모든 함수가 SVG 문자열을 돌려주고 `app.js`만 `innerHTML`을 만진다.
  인쇄에 그대로 실리고 Node에서 단위 검사도 된다(파일 머리말에 그 의도가 적혀 있다).
- **좌표를 계산한다.** `project(x, y, z, u)`가 등각투영을 수식으로 내고, `polygon()`이 점 목록을
  받아 도형을 만든다. 손으로 찍은 좌표가 없다.
- `fmt()`가 소수 둘째 자리로 반올림해 문자열 길이를 억제한다 — 인쇄물 용량에도 유리하다.

따라서 §3의 동작 계열 8종을 만들 때 **SVG를 손으로 쓰지 말고** 이 렌더러의 점·선분 모델을
확장해서 쓴다. 계열마다 필요한 것은 좌표가 아니라 **모델**이다(묶기=그룹 경계, 이사=출발·도착
좌표와 이동량, 쪼개기=자릿값 분할선 …).

## 6-2. 완료 기준 (원장 지시, 2026-08-30)

> "PC·모바일·A4 렌더 화면을 검사하고, 원본 구조·시선·단일 정답이 모두 확인될 때까지
> 완료라고 하지 마."

| 확인할 것 | 어떻게 |
|---|---|
| **PC·모바일·A4 셋 다** | 1280px·430px 실브라우저 + **A4 인쇄 미리보기**. 화면에서 멀쩡한 것이 A4에서 깨진 전례가 있다(`printSteps`가 `p.word`에서 빈 문자열을 돌려주던 결함은 화면에선 안 보였다). print 미디어를 켜고 보는 검사기는 `scripts/check-print-lang.js`뿐이다 |
| **원본 구조** | 그림이 나타내는 수치가 생성기 반환값과 일치하는가(§6-3). 그림이 문제와 다른 수를 그리면 실패 |
| **시선** | 한 beat에 눈이 갈 곳이 하나인가. 강조가 두 곳에 동시에 걸리면 아이는 어디를 봐야 할지 모른다 |
| **단일 정답** | 설계 의도로 끝내지 말고 **기계로 증명**한다 — 답 후보를 전부 훑어 정확히 하나인지 센다. `DV6`가 이 검사를 안 해서 "맞게 쓴 학생이 틀린 것으로 채점"됐다 |

**검사기가 통과했다는 것만으로 완료라고 하지 않는다.** 기능을 하나씩 꺼 보고 검사기가 실제로
잡는지(음성 대조) 확인한 뒤 원복한다 — `check-print.js`와 다국어 작업에서 쓴 방법이다.

### 계보 — 원형은 화이트보드 수업 (원장, 2026-08-30)

`skills/gmap-animated-math-lesson`은 원장이 먼저 만든 **`english-math-whiteboard-lesson`**
(로컬 E드라이브)을 변형한 것이다. **원형이 "선생님이 칠판에 그려 가며 설명하는" 형식**이라는
사실이 G·MAP의 beat 동작(`draw`·`reveal`·`move`·`highlight`)이 왜 그렇게 짜였는지를 설명한다.

그래서 개념 화면의 기본은 **완성된 그림에 강조를 얹는 것이 아니라 빈 칠판에서 하나씩 그려
나가는 것(progressive construction)**이다. 예: A-05는 네 수를 다 띄워 놓고 색을 입히는 대신,
수를 놓고 → 짝을 찾아 잇고 → 100으로 접고 → 남은 짝도 같이.

이건 §6-2의 **시선** 기준과 같은 방향이다 — 칠판 수업에서 아이 눈이 한 곳에 모이는 이유는 강조
색이 하나여서가 아니라 **지금 그려지고 있는 것이 하나**이기 때문이다. 시선을 판단할 때
"이 beat에서 새로 그려지는 것이 하나인가"로 보면 기준이 분명해진다.

**원형에 없는 우리 쪽 제약 하나:** 우리 화면은 영상이 아니라 앱 안의 개념 단계이고 인쇄(A4)로도
나간다. 인쇄물에는 애니메이션이 없으므로 **마지막 상태 한 장이 그 자체로 설명이 되는지**를
반드시 확인해야 한다.

> 참고 스킬: `source-to-memory`(확보: `docssam1/source-to-memory`) ·
> `gfield-single-answer-visibility` · `iterate-until-verified` · `english-math-whiteboard-lesson`.
> 뒤의 셋은 원장 로컬 E드라이브에 있어 2026-08-30 기준 이 세션에서 읽지 못했다(원장이 한 번에
> 정리해 주기로 함). 받으면 이 절을 그 내용으로 대체할 것.

## 6-3. 내레이션은 자막이 기본 — 유료 TTS는 쓰지 않는다 (원장, 2026-08-30)

> "비용이 있으니 TTS 또는 자막으로 한중영어로 표시할 수도 있잖아."

**결론: 자막이 원본 · 브라우저 TTS는 옵션 · 유료 TTS는 지금 안 한다.**

| | 근거 |
|---|---|
| **브라우저 TTS는 이미 3개 언어다** | `app/main.js`의 `_sayWeb(text, lang)`이 `ko-KR`/`en-US`/`zh-CN`을 이미 분기한다. 무료이고 새로 만들 것이 없다 |
| **유료 TTS의 진짜 비용은 재생성비** | 리딩 앱의 `AUDIO_STALE`이 그 전례다 — 지문을 다시 쓴 뒤 MP3가 옛 내용을 읽고 있어서 만든 가드이고, 주석이 `// let TTS read the current text`다. 개념 애니는 147유닛 × 3언어 × beat 6 ≈ **2,600줄**이고 설계 단계라 문장이 계속 바뀐다 |
| **A4에는 소리가 없다** | 자막 텍스트가 곧 인쇄물의 설명이 된다. 한 벌로 화면과 종이를 덮으므로, 위 §계보의 "마지막 상태 한 장이 설명이 되어야 한다"는 제약이 자막이면 저절로 풀린다 |
| **문장 이해력 축과 같은 방향** | 초등은 글을 읽으며 그림을 보는 편이 낫다(`광고-메시지-원칙.md` §1). 소리만 있으면 오히려 역행이다 |
| **텍스트는 낡지 않는다** | 고치면 그걸로 끝. 음성은 다시 뽑아야 한다 |

**유아(NL)만 예외** — 글을 못 읽으므로 TTS가 필수이고, 이미 `say()`로 그렇게 동작한다.

발음 품질이 실제로 문제가 되면 그때 유료 TTS를 **얹으면 된다.** 자막이 원본이면 위에 얹기는
쉽지만, 음성부터 만들면 되돌리기가 어렵다. G·MAP `SKILL.md`도 같은 순서를 권한다
("Browser speech synthesis is acceptable for a zero-cost preview").

## 7. 하지 않는 것

- 문제·정답을 바꾸지 않는다. `scene`은 **덧붙이는 필드**이고 기존 반환값은 불변이다.
- 라이선스 원문(교재 스캔·문장)을 장면에 넣지 않는다 — G·MAP `SKILL.md`의 증거 경계 그대로.
- 스킬을 넘버스 전용으로 포크하지 않는다.
- 잠금을 만들지 않는다(자유 선택 원칙).
