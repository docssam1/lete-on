# 필즈 더 클래식 문제은행 인수인계

## 현재 목표

진단 모의고사의 기존 유사문제는 그대로 둔다. 새 `question-bank`는 실제 선발시험, 필즈 더 클래식 1과정 교재 본문, 권별 단원 테스트를 원본 문항과 1:1로 대조한 뒤에만 무한 생성 문제를 여는 별도 상품이다. 장기적으로는 필즈 더 클래식 전용 목록이 아니라 여러 사고력 교재와 시험을 한 공통 분류 체계로 연결하는 통합 문제은행으로 확장한다.

교재 1과정은 단순 원본 뷰어가 아니다. 각 권과 단원에서 다음을 모두 만든다.

1. 교재 본문 원본 문제 보기
2. 교재 본문과 구조가 같은 유사문제 무한 생성·학습
3. 권별 단원 테스트 원본 문제와 단원 테스트 유사문제 학습

교재 본문과 단원 테스트의 유사문제도 실제 선발시험과 같이 원본 그림·조건·답을 문항별로 대조하고, 쉬움·같게·어려움별 검산을 통과한 유형만 연다.

## 문제은행 UI의 세 가지 구성

메인 화면에서는 아래 세 가지 기준을 서로 분명하게 나누어 제공한다. 세 화면 모두 여러 문항과 여러 유형을 동시에 체크할 수 있어야 한다.

### 1. 시험 종류에 따른 문제은행

- 실제 선발시험, 실전 모의고사, 교재 단원 테스트 등 시험 종류를 먼저 고른다.
- 선택한 시험지의 실제 문항 번호가 1번부터 끝까지 표시된다.
- 사용자는 원하는 원본 문항 번호를 여러 개 체크해 그 문항들과 같은 구조의 문제지 또는 유사문제 학습을 만든다.
- 원본 대조와 생성기 검증이 끝나지 않은 문항은 보이되 선택할 수 없게 한다.

### 2. 시기에 따른 문제은행

- 사용자가 지정한 시험 시기 순서로 선택한다: 6세 12월-7세 2월, 7세 3-5월, 7세 6-8월, 7세 9-12월, 7세 12월-8세 2월, 초1 3-5월, 초1 6-8월, 초1 8-10월, 초1 11월-1월.
- 시기를 선택하면 그 시기에 해당하는 실제 시험지·교재 단원·검증된 세부 유형을 한 화면에 보여 준다.
- 시기별 문제지는 원본 출제 수준을 기준으로 구성하고, 필요하면 `이 문제보다 쉽게·같게·어렵게`를 선택한다.

### 3. 유형에 따른 문제은행

- `대분류 > 중분류 > 세부 유형`의 펼침형 체크 트리를 사용한다.
- 대분류 예: 수와 연산, 규칙과 관계, 논리와 문제해결, 도형과 공간.
- 세부 유형마다 연결된 실제 시험지 이름과 문항 번호를 표시한다.
- 서로 구조가 다른 문항은 이름이 비슷해도 같은 세부 유형으로 합치지 않는다.
- 선택한 여러 유형의 문항 수, 난이도, 출제 순서 또는 섞어서 보기를 설정해 문제지를 만든다.

교재 단원별 선택은 위 세 화면과 별도로 없애는 기능이 아니다. `시험 종류`에서는 단원 테스트로, `시기`에서는 해당 시기의 교재 단원으로, `유형`에서는 해당 단원의 세부 유형으로 각각 접근할 수 있어야 한다.

## 작업 위치와 원본

- 구현: `fields-classic/question-bank/`
- 실제 선발시험 PDF 사본: `tmp/pdfs/source-audit/`
- 실제 선발시험 렌더: `tmp/pdfs/selection-test-render/`
- 1과정 원본: `G:\내 드라이브\1과정(N30)`
- 단원 테스트 렌더: `tmp/pdfs/unit-test-render/book01` ~ `book10`
- 단원 테스트 텍스트: `tmp/pdfs/unit-test-text/book01.txt` ~ `book10.txt`

## 사용자가 지정한 시험지와 시험 시기

아래 시기 구분은 파일명에 보이는 학년·차수와 별도로 사용자가 직접 지정한 기준이다. 문제은행의 시험지별 선택과 난이도 기준은 이 표를 따른다.

| 시험 시기 | 시험지 파일 | 답안 파일 |
| --- | --- | --- |
| 6세 12월-7세 2월 | `더_클래식_7세_1차_선발시험(230206).pdf` | `(답안지)_더_클래식_7세_1차_선발시험(230206).pdf` |
| 7세 3월-5월 | `(최종)더_클래식_7세_2차_선발시험(220418).pdf` | `(최종답안지)더_클래식_7세_2차_선발시험(220418).pdf` |
| 7세 6월-8월 | `더_클래식_7세_3차_선발시험(220622).pdf` | `(답안지)더_클래식_7세_3차_선발시험(220622).pdf` |
| 7세 9월-12월 | `더_클래식_7세_4차_선발시험(220812).pdf` | `답안지_더_클래식_7세_4차_선발시험(220812).pdf` |
| 7세 12월-8세 2월 | `더_클래식_7세_6차_선발시험(221215).pdf` | `(답안지)_더_클래식_7세_6차_선발시험(221215).pdf` |
| 초1 3월-5월 | `더_클래식_1학년_1차_선발시험(20220307).pdf` | `더_클래식_1학년_1차_선발시험(20220307)_답안.pdf` |
| 초1 6월-8월 | `더_클래식_1학년_2차_선발시험(20220530).pdf` | `(답안지)더_클래식_1학년_2차_선발시험(20220530).pdf` |
| 초1 8월-10월 | `더_클래식_1학년_3차_선발시험(20190828).pdf` | `더_클래식_1학년_3차_선발시험(20190821)_답안.pdf` |
| 초1 11월-1월 | `더_클래식_1학년_4차_선발시험(20220215).pdf` | `더_클래식_1학년_4차_선발시험_답안(20220215).pptx` |

연산은 별도 프로그램 범위다. `3더_클래식_2학년_선발시험.pdf`는 초1 11월 연산 테스트이며, 그 이전 연산은 공통 단계로 사용한다.

## 절대 규칙

1. 문항 번호별로 원본 문제지 그림, 조건, 답안지를 모두 대조한다.
2. 제목이 비슷해도 구조가 다르면 다른 세부 유형으로 만든다.
3. 생성기는 답을 코드로 계산하고, 쉬움·같게·어려움은 원본 대비 조건 수와 구조로 조절한다. 숫자 범위만 바꿔 난이도를 표시하지 않는다.
4. 원본 대조와 난수 검산이 끝난 문항만 `verified: true` 및 `sourceMatched: true`로 연다.
5. Golden Bell, 연산 테스트, 2권 이후 교재 뒤 리뷰는 제외한다.
6. `fields-classic/prescription/` 및 `fields-classic/problem-bank/prescription/`의 기존 진단 유사문제는 수정하지 않는다.
7. 문항 제작 세부 기준은 `QUESTION-AUTHORING-GUIDE.md`를 먼저 읽고 따른다.
8. 대·중·세부 유형명은 특정 교재나 시험 이름에 종속시키지 않는다. 출처명·권·단원·문항 번호는 근거 메타데이터로 보존하고, 공통 사고 구조는 통합 유형명으로 관리한다.
9. 같은 공통 유형 안에서도 풀이 구조가 같고 도형 재료만 달라지는 경우에는 변형으로 나눈다. `성냥개비 도형 수열`의 허용 변형은 `삼각형·사각형·오각형·육각형`까지만이며 칠각형 이상은 만들지 않는다. 증가 규칙이나 물어보는 값이 달라지면 별도 세부 유형으로 분리한다.

## 의도적으로 제외하거나 잠가 둔 범위

아래 항목은 누락된 작업이 아니라 사용자가 명시적으로 제외·보류한 범위다. 임의로 다시 연결하거나 기존 자료를 복사해 활성화하면 안 된다.

| 항목 | 현재 처리 | 이유·다시 여는 조건 |
| --- | --- | --- |
| 기존 진단 모의고사 유사문제 | 수정 제외 | 진단 모의고사와 새 문제은행은 별도 상품이다. `fields-classic/prescription/`과 `fields-classic/problem-bank/prescription/`을 새 문제은행 작업으로 수정하지 않는다. |
| 진단지 아래 약점 유형 연습 생성기 | 우선 제외 | 검증되지 않은 유사문제가 서비스에 노출되지 않게 한다. 처방 서비스에서 별도로 승인된 유형만 다시 연결한다. |
| 연산 테스트 | 문제은행 본체에서 제외 | `fields-classic/arithmetic/`의 별도 연산 연습 프로그램으로 관리한다. 초1 11월 연산 자료도 시험 문제은행에 섞지 않는다. |
| Golden Bell 자료 | 전부 제외 | 교재·단원 테스트 문제은행 범위가 아니다. 원본 폴더에 있어도 분석·렌더·생성 대상으로 삼지 않는다. |
| 2권 이후 교재 뒤 리뷰 | 제외 | 교재 본문과 권별 단원 테스트만 사용한다. 리뷰 페이지를 단원 테스트로 오인하지 않는다. |
| 파이널 4회 | 상품·시험지 제작 제외, 서재 자리만 `준비중` | 실전 4회와 문항·그림·수치가 같다. 기존 중복 PDF를 다시 연결하지 말고, 서로 다른 새 시험지를 검수한 뒤에만 활성화한다. |
| 파이널 최종 | 상품·시험지 제작 제외, 서재 자리만 `준비중` | 실전 3회와 문항·그림·수치가 같다. 기존 중복 PDF를 다시 연결하지 말고, 새 시험지 검수 후에만 활성화한다. |
| 파이널 1회 기존 원본 | 그대로 재사용 제외 | 7세 1차와 같은 20유형이다. 검증된 생성기로 수치·보기·그림이 겹치지 않는 교체 시험지를 만든 뒤 문항별 승인받아 연결한다. |
| 7세 2·3·4·6차 미검증 생성기 | 선택 잠금 | 문제·답 분류는 끝났지만 세부 생성 규칙과 난이도별 1,000회 검산이 끝난 문항만 개별적으로 연다. |
| 초1 선발시험 4종 생성기 | 선택 잠금 | 시험지 이름과 시험 시기만 등록된 상태다. 문제지·답지 1:1 대조 전에는 기존의 비슷한 생성기에 억지로 연결하지 않는다. |
| F17~F23 신규 세부 유형 | 선택 잠금 | 유형 분리는 끝났지만 원본·답·렌더·생성기 검증 전에는 열지 않는다. |
| 1과정 교재·단원 테스트 미검증 유형 | 선택 잠금 | 본문과 단원 테스트를 각각 원본 문항별로 대조한 뒤에만 연다. |
| 쌓기나무 임의 색칠·본뜨기 문제 | 제외 | 실제 필즈 문항과 관계없는 색칠 문제를 만들지 않는다. 쌓기나무는 원본 문항과 맞는 전체 개수·숨은 개수·채우기·다른 입체·앞옆위·구멍 유형만 Cube Town 3D 렌더와 연결한다. |
| 원본과 구조가 다른 유사 이미지 | 제외 | 넓은 유형명만 같다는 이유로 PNG·SVG 또는 기존 생성물을 재사용하지 않는다. 원본과 지문·조건·답 계산 구조가 같은 렌더만 사용한다. |

`준비중` 항목은 화면에서 자리는 보일 수 있지만 URL, 학생 권한, 관리자 판매 권한을 주지 않는다. `선택 잠금` 항목은 시험지·유형 목록에는 보여도 체크할 수 없어야 한다.

## 연산 자료의 시험 시기 기준

연산은 이 문제은행 본체가 아니라 별도의 연산 연습 프로그램으로 만든다. 파일명에 적힌 학년만 보고 시험 시기를 판단하면 안 된다.

- `3더_클래식_2학년_선발시험.pdf`는 **초1 11월 연산 테스트**로 사용한다.
- 초1 11월 이전 연산은 공통 연산 단계로 사용한다.
- 초1 선발시험의 시기 구분은 별도 시험지 기준으로 관리한다: 초1 3-5월, 6-8월, 8-10월, 11월-1월.
- 연산 문제를 추가할 때에도 원본 범위, 받아올림·받아내림 여부, 답 형식을 대조한 뒤 생성기로 옮긴다.

## 완료

- 질문은행 UI: 시험지별, 교재 단원별, 대분류 > 중분류 > 세부 유형 선택과 원본 뷰어/워터마크/인쇄 구현.
- 원본 뷰어: `selection-test-viewer.html`, `unit-test-viewer.html`.
- 2023.02.06 7세 1차 20문항: 원본 그림·답지 대조, 라이브 생성 20개, 각 난이도 1,000회씩 총 60,000회 출력 검산 완료.
- 2022.04.18 7세 2차는 20문항을 모두 열었고, 2022.06.22 7세 3차는 17번까지 열었다. 2022.08.12 7세 4차와 2022.12.15 7세 6차는 문제지·답지 20문항을 페이지별로 다시 풀어 대조했지만 생성기는 아직 잠겨 있다.
- 상세 분류표와 답은 `SOURCE-AUDIT.md`에 있다.
- 파이널 1~4회와 파이널 최종 100문항의 대·중·세부 유형, 중복 판정, 난이도 1차 분류는 `FINAL-SOURCE-AUDIT.md`에 있다.
- 파이널 1회는 7세 1차와 중복되어 검증된 유형 생성기로 새 교체 시험지를 만든다. 파이널 4회와 최종은 각각 실전 4회·3회와 중복되어 서재에 `준비중` 자리만 표시한다. 나중에 서로 다른 새 시험지를 검수한 뒤 활성화할 수 있으며, 기존 중복본을 다시 연결하면 안 된다.
- 현재 `fields-classic/program/index.html`에는 파이널 1~4회와 최종 자리가 보이지만, 파이널 4회·최종은 `comingSoon:true`로 열리지 않는다. 관리자 권한 선택과 학생 권한 데이터에서도 제외되어 있다.
- 작업 브랜치 `codex/question-bank-f01`에서 F01~F23을 원본 대조 후 열었다. F01 `a8e8c8b`, F02 `2201052`, F03 `33ed294`, F04 `168a86b`, F05 `6f34d24`, F06 `d48dd38`, F07 `360d956`, F08 `6b20533`, F09 `3860607`, F10 `c2d22c1`, F11 `6dfb754`, F12 `f1950b1`, F13 `241549c`, F14 `0d8934a`, F15 `f3e6017`, F16 `5fd4bde`, F17 `95498a6`, F18 `22fc0aa`, F19 `0e59d23`, F20 `31dad36`, F21 `3e65a4a`, F22 `4d168cf`, F23 `3d482f8`이다. 각 유형은 쉬움·같게·어려움 1,000회씩 총 3,000회 이상의 검산과 브라우저 문제·답안·풀이·콘솔 검사를 통과했다. F01~F08은 파이널 2회, F09~F16은 파이널 3회, F17~F19와 F22는 실전 4회, F20~F21과 F23은 실전 3회에서 처음 확인한 유형이며 아직 `main`에는 병합하지 않았다.
- F05는 원본 `필즈선발대비 실전 모의고사 파이널 2회.pdf` 6쪽 9번을 직접 렌더해 대조했다. 큰 도형·안쪽 도형·안쪽 칠하기를 별도 규칙으로 생성하며, SVG의 각 행·열 속성 유일성, 빈칸 정답, 안팎 도형 판독 가능성을 난이도별 1,000회 검산했다. 모바일 390px에서 가로 넘침 없이 표시된다.
- F06은 같은 원본 6쪽 10번과 해설 답 `6월 3일`을 대조했다. 쉬움은 말한 날짜가 직접 주어지는 한 단계, 같게는 원본의 과거·미래 두 단계, 어렵게는 시험일이 다시 미뤄지는 세 단계로 나누고 월 경계 계산을 난이도별 1,000회 검산했다.
- F07은 원본 7쪽 12번의 목·금·토 3열 달력 조각과 해설 답 `일요일`을 대조했다. 같게는 3열 2주 조각, 쉬움은 7열 1주 조각, 어렵게는 월 중간 3열 1주 조각이며, 표시 날짜·요일과 목표 요일을 난이도별 1,000회 검산했다.
- F08은 원본 7쪽 13번의 두발·세발자전거와 해설 답 `3대`를 대조했다. 쉬움·같게는 자전거 바퀴 수, 어렵게는 파이널 3회 18번과 같은 닭·토끼 다리 수 구조로 나누고, 종류별 수·전체 수·단위 총합을 난이도별 1,000회 검산했다.
- F09는 파이널 3회 원본 3쪽 1번의 4×4 격자와 행 `3·3·4·2`, 열 `4·3·3·2` 조건을 직접 대조했다. 원본 조건에는 가능한 배치가 5개이므로 정답표에는 한 가지 예를 제시하고 다른 올바른 배치도 인정한다. 쉬움 3×3, 같게 4×4, 어렵게 5×5로 격자 구조를 구분했으며, 모든 생성 문제의 행·열 합과 가능한 답 개수를 별도 전수 열거기로 난이도별 1,000회 검산했다. 파이널 3회 20문항 목록은 등록하되 F09 외 문항은 원본 대조가 끝날 때까지 잠겨 있다.
- F10은 파이널 3회 원본 3쪽 2번의 A~E 다섯 문장과 `B, C가 거짓말` 조건을 직접 대조했다. 원본의 가능한 순서는 `C-B-E-A-D` 또는 `B-C-E-A-D`이며 두 경우 모두 E는 3등이다. 대본의 2등 설명은 사용하지 않는다. 쉬움은 4명·거짓말 1명·단순 비교, 같게는 원본처럼 5명·거짓말 2명·복합 관계, 어렵게는 직접 등수 단서 없이 관계만으로 전체 순서가 하나가 되도록 구성한다. 생성 문항마다 모든 순열을 독립 검사해 거짓말/참말 일치와 물어본 등수의 유일성을 난이도별 1,000회 검산했다.
- F11은 파이널 3회 원본 5쪽 9번의 1~5점 동심원 과녁, 화살 두 번, 같은 점수 두 번 허용 조건과 대본 답 `9가지`를 대조했다. 쉬움은 3~4개 연속 점수, 같게는 원본 수준의 5~6개 연속 점수, 어렵게는 일부 점수 간격이 벌어진 5~6개 점수로 구분한다. 가능한 두 발의 합은 같은 점수를 포함해 전수 열거하며, 난이도별 3,000회씩 총 9,000회 독립 검산과 PC·모바일 SVG·답안·풀이 검사를 통과했다.
- F12는 파이널 3회 원본 5쪽 10번의 사각형 `1개=4개, 2개=7개, 10개=31개`와 1과정 2권 70쪽 `규칙 찾기와 수열 > 도형에서 규칙 찾기`의 사각형·오각형 활동을 직접 렌더해 대조했다. 문제은행 중분류는 `수열의 활용`, 유형명은 `성냥개비 도형 수열`이며 삼각형·사각형·오각형·육각형까지만 허용한다. 쉬움은 삼각형·사각형과 증가량 안내, 같게는 사각형·오각형과 첫째·둘째 개수, 어렵게는 오각형·육각형에서 증가량을 직접 찾는다. 난이도별 3,000회씩 총 9,000회 계산 검산과 PC·모바일의 공유 선분 수·정답·풀이·가로 넘침 검사를 통과했다.
- F13은 파이널 3회 원본 6쪽 11번의 원 9개와 선 17개를 직접 대조했다. 원별 연결 수는 `4, 4, 3, 5, 4, 3, 5, 4, 2`이고 합은 대본과 같은 `34`다. 쉬움 6개, 같게 9개, 어렵게 12개 원을 사용하며, 인접한 칸 안에서만 대각선을 연결해 원 없는 교차점이 생기지 않게 했다. 난이도별 3,000회씩 총 9,000회 동안 연결 여부, 중복선·자기 연결·선 교차 없음, 원별 연결 수 합과 정답 일치를 독립 검산했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다.
- F14는 파이널 3회 원본 6쪽 12번의 `소마` 보기와 해설 대본을 대조했다. 원본 글자 픽셀을 실제로 회전해 비교한 결과 조작 순서는 `반시계 방향 90도 회전 → 좌우 뒤집기`다. 쉬움은 조작 순서를 문장으로 안내한 한 글자, 같게는 보기에서 규칙을 찾는 한 글자, 어렵게는 보기에서 규칙을 찾는 두 글자로 구분한다. SVG는 글자를 이미지로 저장하지 않고 같은 변환 행렬을 적용하며, 답안표에는 변환된 그림을 직접 표시한다. 난이도별 3,000회씩 총 9,000회 메타데이터·글자 수·변환 순서를 검산했고 PC·모바일 문제·그림 답안·풀이·가로 넘침 검사를 통과했다. 검수 중 모바일 답안표가 눌리는 기존 문제도 카드형 표로 함께 수정했다.
- F15는 파이널 3회 원본 8쪽 17번의 세 수열을 직접 대조했다. 첫째는 더하는 수가 `1, 2, 3, ...`으로 커져 답 `16`, 둘째는 앞의 두 수를 더해 답 `58`, 셋째는 홀수·짝수 자리를 따로 보는 징검다리 수열로 답 `1`이다. 쉬움은 세 규칙의 도움말과 뒤쪽 빈칸, 같게는 원본과 같은 빈칸 위치 구조, 어렵게는 가운데 빈칸을 앞뒤로 확인하는 구조다. 난이도별 3,000회씩 총 9,000회 동안 증가폭·앞의 두 수 합·두 징검다리 등차를 독립 검산했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다. 모바일 여덟 항이 한 줄에 모두 보이도록 작은 화면에서는 쉼표를 생략한다.
- F16은 파이널 3회 원본 9쪽 20번과 대본의 `전체 40명, 형이 있는 18명, 누나가 있는 8명, 둘 다 3명, 어느 쪽도 아닌 17명`을 대조했다. 쉬움은 두 집합의 각 영역 수를 직접 주고, 같게는 원본처럼 두 집합 전체와 겹침을 주며, 어렵게는 한쪽만 해당하는 수로 겹침부터 구한다. 벤다이어그램에는 집합 전체 수를 원의 특정 영역과 혼동하지 않도록 집합 이름 아래에 따로 표시한다. 난이도별 3,000회씩 총 9,000회 동안 네 영역의 분할, 포함배제 계산, 정답·지문·난이도 구조를 독립 검산했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다.
- F17은 실전 4회 원본 5쪽 7번의 `221 > 2●3 > ★93 > 19◆` 순위표와 답 `세윤 203장, 현희 193장, 도연 192장`을 직접 대조했다. 기존 처방 Q31은 후보 숫자를 먼저 주고 세 명만 비교하므로 원본 대용으로 재사용하지 않았다. 쉬움은 후보 숫자를 제공한 네 명, 같게는 원본처럼 후보 없이 네 명, 어렵게는 후보 없이 다섯 명과 네 개의 가림 숫자로 구분한다. 각 생성 문항은 0~9의 서로 다른 숫자를 모두 대입해 순위가 성립하는 답이 정확히 한 가지인지 난이도별 3,000회씩 총 9,000회 검산했고 PC·모바일 표·답안·풀이·가로 넘침 검사를 통과했다.
- F18은 실전 4회 원본 5쪽 8번의 카드 `5·2·3·4`와 답 `6개`를 직접 대조했다. 구구단이 아니라 두 장을 한 번씩 사용해 두 자리 짝수를 만드는 경우의 수 유형이다. 쉬움은 카드 3장, 같게는 원본과 같은 4장, 어렵게는 0을 포함한 5장으로 구분하며 답이 고정되지 않도록 짝수 카드 수를 바꾼다. 난이도별 3,000회씩 총 9,000회 동안 모든 서로 다른 카드 순서쌍을 직접 열거해 십의 자리 0 제외, 일의 자리 짝수 조건, 답·풀이 목록을 검산했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다.
- F19는 실전 4회 원본 6쪽 12번의 `+4, -6, +5, -3`과 마지막 `9명`, 처음 `9명`을 직접 대조했다. 버스에서 시작 인원을 주고 마지막 인원을 구하는 유형과 분리했다. 쉬움은 두 번, 같게는 원본과 같은 네 번, 어렵게는 여섯 번 오고 가며, 중간 인원이 2명 아래로 내려가는 문제는 만들지 않는다. 난이도별 3,000회씩 총 9,000회 동안 처음 수에서 정방향 계산과 마지막 수에서 역방향 계산을 모두 수행해 답·과정표·풀이를 검산했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다.
- F20은 실전 3회 원본 9쪽 18번의 정확한 식 `27-5+8+10=30`과 지울 부분 `+10`을 직접 대조했다. 쉬움은 뒤의 항 2개, 같게는 원본과 같은 3개, 어렵게는 4개 중 부호와 수 한 덩어리를 지운다. 각 식의 부호가 붙은 항은 서로 다른 값으로 구성한다. 난이도별 3,000회씩 총 9,000회 동안 각 항을 하나씩 모두 지워 목표값이 되는 자리가 정확히 한 곳인지 검산했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다.
- F21은 실전 3회 원본 9쪽 19번의 인접합 `8·9` 반복, 원 수열 `2·6·3·5·4·4·5·3·6·2`, 답 `8개`를 직접 대조했다. 쉬움은 짧은 반복, 같게는 원본 길이, 어렵게는 더 긴 반복과 합의 증가폭 변화로 나눈다. 난이도별 3,000회씩 총 9,000회 동안 모든 인접합의 교대, 시작 수의 첫 재등장 위치, 두 같은 수 사이 개수와 답을 독립 검산했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다. 숫자 뒤 조사가 어색해지지 않도록 지문은 숫자 조사를 쓰지 않는 형태로 고정했다.
- F22는 실전 4회 원본 3쪽 3번의 카드 `5·7·9·11·14`와 연결식 `2+□=□`, `2×□=□`, `□-□=6`을 직접 대조했다. 원본 카드의 모든 순열을 대입하면 `2+9=11`, `2×7=14`, `11-5=6` 한 가지뿐이다. 쉬움은 곱할 수 한 칸을 주고 남은 카드 네 장, 같게는 원본과 같은 다섯 빈칸, 어렵게는 곱셈 결과에서 다시 빼는 연결식과 여섯 번째 카드를 추가한다. 난이도별 3,000회씩 총 9,000회 동안 모든 카드 순열을 독립 전수 대입해 답의 유일성, 카드 중복 없음, 모든 식 성립을 검산했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다.
- F23은 실전 3회 원본 8쪽 17번의 카드 `4·0·2·7·1`과 30보다 작은 두 자리 수 `10·12·14·17·20·21·24·27`을 직접 대조했다. 한 수를 만들 때 같은 카드를 두 번 쓸 수 없고 십의 자리에 0을 놓을 수 없다. 쉬움은 카드 네 장과 한쪽 경계에서 답 3~6개, 같게는 카드 다섯 장과 원본 구조에서 답 8~12개, 어렵게는 카드 여섯 장과 두 경계 사이에서 답 5~14개로 나눈다. 난이도별 3,000회씩 총 9,000회 동안 모든 서로 다른 카드 순서쌍을 독립 열거해 누락·중복·경계 위반이 없는지 검산했고 PC·모바일 문제·긴 답칸·답안·풀이·가로 넘침 검사를 통과했다. 검수 중 중복되던 보조 답줄은 없애고 긴 공통 답칸 하나만 남겼다.
- 7세 2차 1번은 원본 3쪽의 카드 `2·3·4·5`, 변의 합 `위9·오른6·아래5·왼8`, 유일한 전체 배치 `왼위5·오위4·오아래2·왼아래3`을 직접 대조했다. 7세 1차에서 이미 검증한 `edgeSumCycle`이 보기·네 원·네 변 합·전체 배치 답 형식까지 같아 재사용했다. 원본과 생성 문제의 모든 카드 순열을 검사하고 난이도별 3,000회씩 총 9,000회 유일성을 재검산했으며 PC·모바일에서 1번만 열리고 2번 이후가 잠기는지 확인했다.
- 7세 2차 2번은 원본 3쪽의 구슬 `13개·5개`와 답 `4개`를 직접 대조했다. 7세 1차에서 검증한 `equalizeTransfer`와 같은 “많은 쪽이 적은 쪽에 몇 개를 주어 같게 만드는가” 구조라 재사용했다. 난이도별 3,000회씩 총 9,000회 동안 두 수의 차가 이동 수의 두 배인지, 이동 뒤 두 수가 같은지, 답과 그림 개수가 일치하는지 검산했다. 검수 중 기존 이름 조합에서 `민서이가`가 나올 수 있는 조사 오류를 발견해 이름 조사 없이 읽히는 유아용 문장으로 수정했고 PC·모바일 문제·답안·풀이·가로 넘침 검사를 통과했다.
- 7세 2차 3번은 원본 4쪽의 카드 `1·2·3·4·5`, 꼭대기 `9`, 가운데 아래칸 `㉠=3`을 직접 대조했다. 기존 `numberPyramid`는 카드 세 장을 한 층에만 놓는 다른 유형이므로 재사용하지 않고 `fiveCardSumPyramid`를 새로 만들었다. 쉬움은 아래 한 칸을 주고 남은 카드 네 장, 같게는 원본처럼 다섯 칸 모두 빈칸, 어렵게는 카드 여섯 장 중 다섯 장을 고르는 구조다. 원본에는 좌우 대칭 두 배치가 있지만 ㉠은 모두 3이다. 난이도별 3,000회씩 총 9,000회 동안 가능한 모든 카드 배치를 독립 열거해 ㉠의 유일성, 어려움의 쓰지 않는 카드 유일성, 각 층의 합을 검산했고 PC·모바일 SVG·답안·풀이·가로 넘침 검사를 통과했다.
- 7세 2차 4번은 원본 4쪽을 확대해 정사각형 2×2가 아니라 `위 가운데·위 오른쪽·아래 왼쪽·아래 가운데`의 계단형 4칸임을 재확인했다. `4의 왼쪽에 2`, `4는 3의 바로 아래` 조건이면 아래 가운데 4, 아래 왼쪽 2, 위 가운데 3이 되고 표시된 위 오른쪽은 남은 `1`이다. `stairGridPlacement`를 새로 만들었으며 쉬움은 한 칸 제공, 같게는 원본 네 카드, 어렵게는 가장 큰 방해 카드 한 장을 빼고 네 카드를 배치한다. 난이도별 3,000회씩 총 9,000회 동안 모든 카드 순열을 독립 검사해 전체 배치와 ㉠의 유일성, 방해 카드 조건을 검산했고 PC·모바일 계단형·답안·풀이·가로 넘침 검사를 통과했다.
- 7세 2차 5번은 원본 5쪽의 `5명`, `지우는 뒤에서 두 번째`, `지우와 민호 사이 한 명`, 답 `2등`을 직접 대조했다. 기존 `raceOrder`는 고정 등수와 앞선 사람을 조합하는 다른 유형이므로 `orderPositionFromBack`을 별도로 만들었다. 쉬움은 4명·사이 한 명, 같게는 원본 5명·사이 한 명, 어렵게는 7명·사이 두 명으로 위치 관계 자체를 조절한다. 난이도별 3,000회씩 총 9,000회 동안 가능한 등수를 독립 열거해 답이 하나인지 검산했다. 화면 검수에서 `예린는`, `예린와` 같은 이름 조사 오류를 발견해 한글 받침 판별 도우미를 추가했고, 수정 후 PC·모바일 10문항·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 6번은 원본 5쪽의 4×4 표를 직접 대조해 첫째 줄 `2·4·6·8`, 가로 `+2`, 세로 `+3`, 오른쪽 아래 `㉠=17`을 재확인했다. `additionTableGrid`를 새로 만들었으며 쉬움은 3×3과 많은 단서, 같게는 원본과 같은 4×4 단서 위치, 어렵게는 5×5와 적은 단서로 구조를 조절한다. 난이도별 3,000회씩 총 9,000회 동안 가능한 시작 수·가로 증가·세로 증가를 독립 열거해 규칙과 답이 하나인지 검산했다. 답안 검수에서 `2을 3번`을 발견해 `2씩 3번`으로 고쳤고, PC·모바일 10문항·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 7번은 원본 6쪽의 도형 배치 `◇+○`, `◇+□`, 아래 합 `◇+◇`, 목표 `○+□`를 직접 대조했다. 기존 `shapeSumTable`은 윗줄이 `◇+◇`인 다른 배치라 재사용하지 않고 세부 유형 `shape-sum-table-bottom-target`과 `shapeSumBottomTarget`을 새로 만들었다. 원본 값 `◇+○=11`, `◇+□=9`, `◇+◇=14`에서 답 `○+□=6`을 독립 계산으로 확인했다. 쉬움은 마름모 값 제공, 같게는 원본 [보기] 제공, 어렵게는 [보기]를 빼는 방식으로 발판을 조절했다. 난이도별 3,000회씩 총 9,000회 동안 세 도형의 값과 목표 합을 역산했고 PC·모바일 10문항·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 8번은 원본 6쪽의 `세모·동그라미·마름모` 3개 반복마디와 11번째 답 `동그라미`를 직접 대조했다. 기존 `repeatShapeSequence`는 `동그라미·세모·동그라미`로 같은 모양이 반복마디 안에 두 번 나오는 다른 유형이므로 재사용하지 않고 `threeShapeCycle`을 새로 만들었다. 무한 생성에서는 세모·동그라미·마름모·네모·별 중 서로 다른 세 모양과 순서를 뽑아 같은 난이도에서도 60가지 반복마디가 나오게 했다. 쉬움은 반복마디 안내, 같게는 원본처럼 7개 제시, 어렵게는 더 먼 번째를 묻는다. 난이도별 3,000회씩 총 9,000회 동안 반복 위치와 답을 검산했고 PC·모바일 10문항·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 9번은 원본 7쪽의 `[보기]` 좌우 `±1`, 위 `-10`, 아래 `+10` 규칙과 `46 → 45 → 35 → 34 → 24 → 25 → 26 → 16`의 7칸 경로를 직접 대조했다. 기존 `arrowNumberGrid`는 8번 이동하는 다른 경로이므로 재사용하지 않고 세부 유형 `arrow-number-path-seven`과 `arrowNumberPathSeven`을 새로 만들었다. 쉬움 5칸, 같게 7칸, 어렵게 9칸으로 경로 길이를 조절하며 숫자만 키우지 않는다. 난이도별 3,000회씩 총 9,000회 동안 방향 벡터, 칸별 증감, 경로 점 중복 없음, 정답을 독립 검산했고 PC·모바일 10문항·화살표 수·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 10번은 원본 7쪽의 시작 `27명`, 첫 정류장 `12명 하차`, 두 번째 정류장 `9명 승차`, 답 `24명`을 직접 대조했다. 기존 `sourceBusStops`가 시작 인원 제시, 내림 후 탑승 순서, 조건 상자, 마지막 인원 질문까지 원본과 같아 그대로 재사용했다. 쉬움·같게·어려움 각각 시작 인원과 승하차 범위를 달리하며, 난이도별 3,000회씩 총 9,000회 동안 `시작-하차+승차`를 독립 계산하고 중간·최종 인원이 양수인지 확인했다. PC·모바일 10문항·답안·풀이·가로 넘침·콘솔 검사도 통과했다.
- 7세 2차 11번은 원본 문제지·답안지 8쪽의 4×4 숫자표, 왼쪽 아래에서 오른쪽 위로 접는 대각선, 칠한 네 부분, 답 `39`를 직접 대조했다. 원본 배열에서 대각선 위 세 칸은 맞은편 세 칸과 함께 잘리고 대각선에 걸친 한 칸은 한 번만 잘려 `4+5+8+7+6+2+7=39`가 된다. `foldNumberCutSum`은 1~8을 두 번씩 섞은 표와 대각선 반사 좌표로 답을 계산하며, 쉬움 3칸·같게 7칸·어렵게 9칸이 잘리도록 칠한 부분의 구조를 조절한다. 난이도별 3,000회씩 총 9,000회 동안 숫자 카드 구성, 반사 좌표, 중복 제거, 잘린 수의 합을 독립 검산했고 PC·모바일 10문항·접기 그림·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 12번은 원본 문제지·답안지 8쪽의 1~8 카드, 3×3 테두리 여덟 자리, 각 변의 합 `15`, 모서리 단서 `왼쪽 위 7·왼쪽 아래 6·오른쪽 아래 8`, 오른쪽 위 ㉠과 답 `3`을 직접 대조했다. 가능한 1~8 순열 40,320개 중 네 변의 합이 같은 48개 배열을 전수 열거했으며 원본 단서를 만족하는 배열은 하나다. 쉬움은 한 변의 가운데칸과 양쪽 모서리를 보여 바로 뺄 수 있고, 같게는 원본처럼 다른 세 모서리로 목표 모서리를 찾고, 어렵게는 답은 하나지만 전체 배열은 여러 개 남는 혼합 단서를 준다. 난이도별 3,000회씩 총 9,000회 동안 카드 중복 없음, 네 변 합, 단서 일치, ㉠ 값의 유일성을 독립 검산했다. 모바일에서 숫자 카드 줄이 문항 내부를 41px 넘는 문제를 발견해 카드 크기와 간격을 줄인 뒤 PC·모바일 문제·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 13번은 원본 문제지·답안지 9쪽의 첫째 줄 `네모·네모·네모=12`, 둘째 줄 `네모·마름모·세모=14`, 셋째 줄 `마름모·동그라미·동그라미=14`, 아래 합 `10·12·㉠`과 답 `18`을 직접 대조했다. 기존 `sourceSymbolSumGrid`는 별이 들어가고 각 행의 배치가 달라 재사용하지 않고 `symbol-sum-grid-square-top`과 `symbolSumGridSquareTop`을 새로 만들었다. 쉬움은 네모 값을 알려 주고, 같게는 원본의 다섯 합을 모두 보여 주며, 어렵게는 중복 확인용 둘째 세로 합을 숨긴다. 난이도별 3,000회씩 총 9,000회 동안 네 도형 값의 구별, 세 행·세 열 합, 목표 열 합을 독립 검산했고 원본 값 `네모4·마름모2·동그라미6·세모8`에서 `18`을 재현했다. PC·모바일 10문항·답안·풀이·가로 넘침·콘솔 검사도 통과했다.
- 7세 2차 14번은 원본 문제지·답안지 9쪽의 `[보기] ④+5=9, 7-④=3`, 본문 `□+28=63, 53-□=♡`와 답 `18`을 직접 대조했다. `shapeEquationAddSubtract`와 원본처럼 보기·본문을 나눈 식 패널을 새로 연결했다. 쉬움은 받아올림·받아내림 없는 한 자리 두 단계, 같게는 원본처럼 받아올림의 역산과 받아내림이 필요한 두 자리 두 단계, 어렵게는 하트에서 별까지 한 식을 더 잇는 세 단계다. 난이도별 3,000회씩 총 9,000회 동안 모든 식과 답을 독립 재계산했고 원본 `63-28=35, 53-35=18`을 재현했다. 브라우저 답안 검수에서 `28를`처럼 틀리던 숫자 조사를 발견해 끝자리 받침 헬퍼로 고쳤으며 PC·390px 모바일 문제·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 15번은 원본 문제지·답안지 10쪽의 `60보다 크고 90보다 작은 짝수`, `십의 자리 숫자가 일의 자리 숫자보다 5 큼`과 답 `72`를 직접 대조했다. `twoDigitParityGap`은 10부터 99까지 모든 수를 독립 열거해 범위·짝수·자릿수 차 조건의 유일해를 확인한다. 쉬움은 조건에 맞는 수를 세 보기에서 고르고, 같게는 원본처럼 범위·짝수·차 조건만으로 찾고, 어렵게는 범위를 넓혀 차 조건 후보를 둘 이상 남긴 뒤 자릿수 합 조건으로 하나를 고른다. 난이도별 3,000회씩 총 9,000회 동안 유일성·보기 포함·어려움의 복수 중간 후보를 검산했고 원본에서 `72` 하나만 남음을 재현했다. 답안 검수에서 `차가 2이 되는` 문장을 발견해 `차가 2인 수`로 고쳤으며 PC·390px 모바일 세 난이도·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 16번은 원본 문제지·답안지 10쪽의 작은 정삼각형 배열 `1·4·9·16`, 8번째와 답 `64장`을 직접 대조했다. `triangleTileGrowth`와 선명한 SVG 삼각 격자를 새로 만들었고 각 1~4단계 그림의 선분 수가 각각 `3·9·18·30`인지도 검사했다. 쉬움은 첫 네 그림 아래에 `1·4·9·16장`을 보여 주고, 같게는 원본처럼 그림과 번째만 보여 주며, 어렵게는 주어진 장수로 몇 번째 모양인지 거꾸로 찾는다. `제곱`이나 위첨자 표기는 쓰지 않고 “8을 8번 더한 64”처럼 설명한다. 난이도별 3,000회씩 총 9,000회 동안 단계·조각 수·정방향과 역방향 답을 검산했고 PC·390px 모바일 세 난이도·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 17번은 원본 문제지·답안지 11쪽의 세 양팔저울을 그림별로 대조했다. 원본은 `○=□+☆`, `○+□=◇+☆`, `☆=□+□+◇`이므로 ◇는 □ 2개, ☆는 □ 4개, ○는 □ 5개와 같다. 기존 기억 색인에 답이 `4`로 적힌 오류를 발견했지만 공식 답안의 `5`와 독립 치환 계산이 일치해 `5개`로 바로잡았다. 기존 `sourceBalanceRelations`는 긴 네모와 원의 다른 관계이므로 건드리지 않고 `balanceScaleThreeObjects`를 새로 연결했다. 쉬움은 ◇의 중간 관계를 도움말로 보여 주고, 같게는 원본과 같은 세 관계로 ○를 구하며, 어렵게는 ○와 ◇를 합친 무게까지 한 단계 더 구한다. 난이도별 3,000회씩 총 9,000회 동안 세 저울의 양변과 답을 독립 계산했고 답 다양성도 확인했다. PC·390px 모바일 세 난이도·20문항 답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 18번은 원본 문제지·답안지 11쪽의 `☆+☆=○+○+○`, `☆+○=▽+▽`, `○+▽=□`와 답 `9`를 직접 대조했다. 0부터 9까지 서로 다른 세 수를 전수 대입하면 `☆=6, ○=4, ▽=5` 한 가지뿐이다. 기존 `sourceSymbolRelations`는 난이도에 따라 첫 관계까지 달라지는 다른 유형이므로 `symbolRelationTwoToThree`를 별도로 연결했다. 첫 두 관계는 모든 난이도에서 원본 그대로 유지하고, 쉬움은 첫 두 값을 도움말로 제공하며, 같게는 두 기호를 더하고, 어렵게는 세 기호를 더해 □를 구한다. 마지막 식의 기호 조합을 바꿔 정답도 `9·10·11` 또는 `13~16`으로 다양화했다. 난이도별 3,000회씩 총 9,000회 동안 식과 답을 독립 계산했다. 답안 검수에서 첫째 식만으로 두 값이 정해진다고 잘못 설명한 문장을 발견해 둘째 식까지 함께 확인하는 유일해 풀이로 고쳤고, PC·390px 모바일·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 19번은 원본 문제지·답안지 12쪽의 여섯 점 `A-B-C-D-E-F`, 겹친 거리 `AC=27, AD=47, BD=30, CE=54, BF=84`, 목표 `EF`와 답 `20`을 직접 대조했다. `CD=47-27=20`, `BC=30-20=10`, `DE=54-20=34`, `EF=84-(10+20+34)=20`으로 독립 확인했다. `numberLineSixPoints`와 점·점선 호·거리 값을 그리는 SVG를 새로 연결했다. 쉬움은 BC와 CD를 도움말로 제공하고, 같게는 원본의 다섯 겹친 거리로 EF를 구하며, 어렵게는 모든 간격을 구한 뒤 AF 전체까지 계산한다. 난이도별 3,000회씩 총 9,000회 동안 다섯 간격과 표시 거리, 답을 독립 재계산했고 정답 다양성도 확인했다. 모바일에서 거리 숫자가 작아지는 문제와 풀이의 `11를`, `20와` 같은 숫자 조사 오류를 발견해 글자를 키우고 `을/를·과/와`를 끝소리에 맞게 고쳤다. PC·390px 모바일·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 2차 20번은 원본 문제지·답안지 12쪽의 1~5번째 삼각 바둑돌 배열, 검은 돌이 흰 돌보다 `17개` 많은 때와 답 `33번째`를 직접 대조했다. 위에서부터 1·3·5…번째 줄은 검은 돌, 2·4·6…번째 줄은 흰 돌이어서 검은 돌이 더 많은 1·3·5…번째의 차는 1·2·3…개가 된다. `goStoneDifferenceInverse`와 다섯 단계의 줄별 흑백 돌을 그리는 HTML 렌더러를 새로 연결했다. 쉬움은 검은 돌이 더 많은 홀수 번째 순서를 도움말로 제공하고, 같게는 원본처럼 검은 돌의 차로 홀수 번째를 찾으며, 어렵게는 검은 돌 또는 흰 돌의 차를 보고 홀수·짝수 번째를 판단한다. 난이도별 3,000회씩 총 9,000회 동안 각 단계의 흑백 개수를 직접 세고 목표 차가 처음 나타나는 번째를 1~100까지 전수 탐색해 답을 검산했다. 원본 `17→33`을 재현했으며 PC·390px 모바일·20문항 답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 1번은 원본 문제지·답안지 3쪽의 카드 `1·2·4·8`, 변의 합 `위5·오른9·아래10·왼6`, 전체 배치 `왼위4·오위1·오아래8·왼아래2`를 직접 대조했다. 기존 `edgeSumCycle`의 실제 난이도 그림은 원본 구조와 같지만 난이도가 수 범위로만 달라지는 문제를 발견해 구조형 난이도로 고쳤다. 쉬움은 한 원의 수를 미리 주고, 같게는 카드 네 장을 모두 쓰며, 어렵게는 카드 다섯 장 중 네 장을 고른다. 원본 24개 배치와 난이도별 생성 문제를 전수 대입해 유일성을 확인했고, 난이도별 3,000회씩 총 9,000회 검산과 PC·390px 모바일·답안·풀이·가로 넘침·콘솔 검사를 통과했다. 답안 검수에서 `합이 12이 되는` 같은 숫자 조사 오류도 숫자를 반복하지 않는 문장으로 고쳤다.
- 7세 3차 2번은 원본 문제지·답안지 3쪽의 `동그라미·세모·네모·마름모` 네 모양 반복과 14번째 답 `세모`를 직접 대조했다. 세 모양 반복 생성기를 재사용하지 않고 `fourShapeCycle`을 새로 만들었으며, 같은 난이도에서도 세모·동그라미·마름모·네모·별·오각형·육각형 중 서로 다른 네 모양과 순서가 바뀐다. 쉬움은 반복마디를 표시하고 8개를 보여 주며, 같게는 원본처럼 9개를 보여 주고, 어렵게는 한 반복마디만 보고 먼 번째를 찾는다. 난이도별 3,000회씩 총 9,000회 동안 반복 위치와 답을 검산했고 840가지 순서를 확인했다. 모바일에서 반복 줄이 잘리는 문제를 발견해 도형·번호·간격을 조정한 뒤 PC·390px 모바일·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 3번은 원본 문제지·답안지 4쪽을 직접 대조해 `윗줄 2칸 + 오른쪽 아래 2칸`인 ㄱ자 4칸, 조건 `3의 왼쪽에 2`, `1은 4 바로 아래`, 표시 칸 답 `4`를 확인했다. 기존 `stairGridPlacement`는 계단형 4칸이라 재사용하지 않고 세부 유형 `l-grid-placement`와 `lGridPlacement`를 새로 만들었다. 쉬움은 한 칸 제공, 같게는 원본의 네 카드와 두 관계, 어렵게는 다섯 카드 중 네 장을 고르고 세 관계를 읽는다. 고정 격자의 모든 카드 순서를 전수 대입하고 난이도별 3,000회씩 총 9,000회 유일성을 검산했다. 답안 검수에서 `4이 있습니다` 같은 숫자 조사 오류를 발견해 조사 없는 조건 형식으로 고쳤고 PC·390px 모바일·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 4번은 원본 문제지·답안지 4쪽의 `5명`, `지우는 앞에서 2번째`, `지우와 민호 사이 1명`, 답 `4등`을 직접 대조했다. 뒤에서 세는 `orderPositionFromBack`과 구분해 `orderPositionFromFront`를 새로 만들었다. 쉬움은 4명·앞에서 1번째·사이 1명, 같게는 원본의 5명·앞에서 2번째·사이 1명, 어렵게는 7명·앞에서 3번째·사이 2명으로 순서 관계를 조절한다. 가능한 모든 등수를 열거해 각 문제의 답이 하나인지 난이도별 3,000회씩 총 9,000회 검산했고, PC·390px 모바일·답안·풀이·한글 조사·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 5번은 원본 문제지·답안지 5쪽의 `큐브 20개`, `윤서가 다빈이보다 4개 더 많이 갖기`, 답 `12개`를 직접 대조했다. 기존 문항 연결에서 5번과 6번이 서로 바뀐 오류를 발견해 5번을 `total-difference`, 6번을 `five-card-sum-pyramid`로 바로잡았다. `totalDifferenceShare`는 전체 수에서 차이를 먼저 덜어 낸 뒤 남은 수를 똑같이 나누는 구조이며, 큐브 그림의 개수도 지문의 전체 수와 항상 일치한다. 쉬움은 차이 2개와 시각 도움말, 같게는 원본의 전체 수·차이 조건, 어렵게는 한 사람이 큐브를 준 뒤에도 남는 차이에서 처음 수를 거꾸로 구한다. 난이도별 3,000회씩 총 9,000회 동안 전체·차이·이동 뒤 차이·정답을 독립 계산해 검산했고 PC·390px 모바일·큐브 수·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 6번은 원본 문제지·답안지 5쪽의 카드 `2·3·4·5·6`, 꼭대기 `11`, 아래 가운데 ㉠과 답 `2`를 직접 대조했다. 7세 2차 3번에서 만든 `fiveCardSumPyramid`와 같은 구조라 재사용했다. 원본 카드의 가능한 배치는 좌우 대칭 두 가지지만 ㉠은 모두 2이며, 생성 문제도 가능한 모든 카드 배치를 전수 열거해 ㉠ 값이 하나일 때만 출제한다. 쉬움은 아래 한 칸 제공, 같게는 원본처럼 다섯 칸 모두 빈칸, 어렵게는 카드 여섯 장 중 다섯 장을 골라 쓰는 구조다. 난이도별 3,000회씩 총 9,000회 동안 카드 중복·각 층 합·㉠ 유일성·어려움의 제외 카드 유일성을 검산했다. 답안 화면에서 `15이 됩니다`, `6로 놓으면` 같은 숫자 조사 오류를 발견해 `이/가·으로/로`를 끝소리에 맞게 고쳤고 PC·390px 모바일·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 7번은 원본 문제지·답안지 6쪽의 4×4 표를 직접 대조해 첫째 줄 `3·6·9·12`, 가로 `+3`, 세로 `+4`, 오른쪽 아래 ㉠과 답 `24`를 재확인했다. 7세 2차 6번에서 만든 `additionTableGrid`의 4×4 단서 위치가 원본과 같아 재사용했다. 쉬움은 3×3과 많은 단서, 같게는 원본과 같은 4×4 단서, 어렵게는 5×5와 적은 단서로 구조를 조절한다. 난이도별 3,000회씩 총 9,000회 동안 가능한 시작 수·가로 증가·세로 증가를 독립 열거해 단서와 목표 답이 하나인지 검산했고, 원본 `3,+3,+4→24`도 별도 재계산했다. PC·390px 모바일·표 칸 수·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 8번은 원본 문제지·답안지 6쪽의 [보기]와 오른쪽 표를 직접 대조했다. 원본 배치는 위 행 `◇·○=13`, 아래 행 `□·□=18`, 둘째 열 `○+□=16`, 목표 첫째 열 `◇+□=15`다. 기존 `shapeSumTable`은 `◇◇ / □○`에서 둘째 행의 합을 묻는 다른 유형이라 재사용하지 않고 `shape-sum-table-column-target`과 `shapeSumColumnTarget`을 새로 만들었다. 쉬움은 네모 값을 도움말로 제공하고, 같게는 원본처럼 [보기]와 네 합을 사용하며, 어렵게는 [보기] 없이 색칠된 합 칸의 뜻을 이해해 푼다. 난이도별 3,000회씩 총 9,000회 동안 세 도형 값을 전수 역산해 유일성과 네 합·정답을 검산했고 원본 `◇6·○7·□9→15`를 재현했다. PC·390px 모바일·도형 위치·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 9번은 원본 문제지·답안지 7쪽의 [보기] `왼쪽 -1·오른쪽 +1·위 -10·아래 +10`, 출발 `66`, 이동 `위→위→오른쪽→아래→오른쪽→오른쪽→위`, 답 `49`를 직접 대조했다. 고정 8번 이동인 `arrowNumberGrid`는 다른 유형이라 연결하지 않고 7번 이동인 `arrowNumberPathSeven`을 재사용했다. 같은 난이도에서도 경로가 달라지도록 고정 경로를 겹치는 점이 없는 무작위 격자 경로로 개선했고, 같게는 7번 이동·세 방향 이상·아래쪽 포함, 쉬움은 5번 이동·두 방향 이상, 어렵게는 9번 이동·네 방향 모두 사용한다. 난이도별 3,000회씩 총 9,000회 동안 점 중복 없음·방향 벡터·칸별 증감·양수·정답을 독립 검산했으며 280·1,099·2,053가지 경로를 확인했다. 답안 풀이에서 아래쪽 `+10` 설명이 빠진 문제를 발견해 고쳤고 PC·390px 모바일·화살표 7개·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 10번은 원본 문제지·답안지 7쪽의 `24명 출발 → 첫 정류장 8명 승차 → 둘째 정류장 14명 하차`, 답 `18명`을 직접 대조했다. 기존 `sourceBusStops`는 7세 2차 10번의 `먼저 하차 → 다음 승차` 순서라 원본과 반대였으므로 재사용하지 않고 세부 유형 `bus-board-then-leave`와 생성기 `busBoardThenLeave`를 분리했다. 쉬움은 두 정류장과 첫 정류장 뒤 인원 도움말, 같게는 원본과 같은 승차·하차 두 단계, 어렵게는 승차·하차·승차 세 단계로 구조를 조절한다. 난이도별 3,000회씩 총 9,000회 동안 사건 순서·중간 인원·최종 양수·정답을 독립 검산했고 360·1,287·2,764가지 조합을 확인했다. 기존 하차·승차 생성기도 회귀 검사해 그대로 유지했으며 PC·390px 모바일·답안 10개·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 11번은 원본 문제지·답안지 8쪽의 도형 배치 `△□○ / ◇□○ / ○△○`, 오른쪽 합 `㉠·18·15`, 아래 합 `14·17·18`, 답 `16`을 직접 대조했다. 기존 `sourceSymbolSumGrid`는 `☆□△ / □□☆ / △□○`인 다른 배치라 재사용하지 않고 `shape-sum-grid-top-target`과 `shapeSumGridTopTarget`을 새로 만들었다. 또한 현재 11번에 테두리 합, 12번에 도형 합이 연결된 순서 오류를 발견해 원본 순서인 11번 도형 합·12번 테두리 합으로 바로잡았다. 쉬움은 동그라미 값을 도움말로 제공하고, 같게는 원본처럼 다섯 합을 보이며, 어렵게는 둘째 세로줄 합을 숨겨 둘째 가로줄과 첫째 세로줄의 공통 도형을 비교해 네모를 찾도록 했다. 난이도별 3,000회씩 총 9,000회 동안 서로 다른 한 자리 수 3,024가지 배치를 전수 비교해 보이는 단서의 해와 목표 합이 하나인지 검산했고 1,918·1,881·1,920가지 조합을 확인했다. PC·390px 모바일·답안 10개·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 12번은 원본 문제지·답안지 8쪽의 카드 `1~8`, 네 변의 합 `12`, 왼쪽 위 `3`·오른쪽 위 `2`·왼쪽 아래 `1`, 오른쪽 아래 ㉠과 답 `6`을 직접 대조했다. 기존 `equalLineSumEightCards`의 실제 난이도는 합 `15`와 다른 목표 위치를 쓰므로 공통 생성 로직을 분리하고 `equal-line-sum-eight-cards-twelve`와 `equalLineSumEightCardsTwelve`를 추가했다. 쉬움은 목표가 변 가운데에 있고 양끝 수를 보여 한 번 빼기로 풀며, 같게는 원본처럼 세 모서리를 보여 나머지 모서리를 찾되 ㉠ 위치를 네 모서리에서 바꾸고, 어렵게는 가능한 배치가 둘 이상이어도 ㉠ 값만 하나가 되도록 단서를 고른다. 난이도별 3,000회씩 총 9,000회 동안 1~8의 40,320가지 순서를 독립 전수해 네 변의 합·카드 중복·단서 일치·㉠ 유일성을 검산했고 쉬움 96·같게 32·어렵게 64가지 출제 구성을 확인했다. 원본 배치 `3·7·2·4·6·5·1·8→6`과 기존 합 15 생성기도 별도 회귀 검사했으며 PC·390px 모바일·답안 10개·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 13번은 원본 문제지·답안지 9쪽의 `[보기]`, `□+26=54`, `42-□=♡`, 답 `14`를 직접 대조했다. 기존 `shapeEquationAddSubtract`가 같은 두 단계 구조와 받아올림 조건을 사용하므로 재사용했다. 쉬움은 한 자리 수 두 단계, 같게는 원본처럼 받아올림이 있는 두 자리 수 두 단계, 어렵게는 하트를 찾은 뒤 별까지 구하는 세 단계다. 난이도별 3,000회씩 총 9,000회 동안 첫 식의 덧셈·둘째 식의 뺄셈·받아올림·목표 도형·정답을 독립 검산했고 135·1,935·2,964가지 식을 확인했다. 원본 `54-26=28`, `42-28=14`도 별도 계산했으며 PC·390px 모바일·답안 10개·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 14번은 원본 문제지·답안지 9쪽의 `30보다 작은 홀수`, `십의 자리 숫자가 일의 자리 숫자보다 5 작음`, 답 `27`을 직접 대조했다. 기존 `twoDigitParityGap`은 짝수를 찾는 다른 유형이므로 재사용하지 않고 `two-digit-odd-gap`과 `twoDigitOddGap`을 분리했다. 쉬움은 세 보기에서 고르고, 같게는 원본처럼 범위와 자리 숫자 차로 하나를 찾으며, 어렵게는 조건을 만족하는 홀수 2~3개를 모두 찾아 더한다. 난이도별 3,000회씩 총 9,000회 동안 10부터 99까지 직접 대입해 후보 수·유일성·합·보기 포함을 검산했고 원본 `27`을 재현했다. 브라우저 검수에서 한 학습지에 같은 조건이 중복될 수 있는 문제를 발견해 지문과 그림이 같은 문항은 최대 80번 다시 뽑도록 공통 중복 방지를 추가했다. PC 문제 10개·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 다음 원본 10쪽을 문제지와 공식 답안으로 직접 대조해, 15번은 정사각형 배열의 7번째 `49개`, 16번은 한 번 접은 4x4 번호 색종이의 잘린 수 합 `18`임을 확인했다. 코드에서 두 유형 번호가 서로 바뀌어 있던 오류를 바로잡았으며 두 문항은 생성기 검증 전까지 계속 잠겨 있다.
- 7세 3차 15번은 원본 문제지·답안지 10쪽의 작은 정사각형 배열 `1·4·9·16`, 7번째와 답 `49개`를 직접 대조했다. 중분류를 단순 도형 성장이 아닌 `수열의 활용`, 유형명을 `정사각형 배열의 수 규칙`으로 정리하고 `squareTileGrowth`와 선명한 SVG 정사각형 격자를 연결했다. 쉬움은 첫 네 그림 아래에 개수를 보여 주고, 같게는 원본처럼 그림과 번째만 보여 주며, 어렵게는 주어진 장수로 몇 번째인지 거꾸로 찾는다. `제곱`이나 위첨자 표기를 사용하지 않고 한 줄의 장수와 줄 수로 설명한다. 난이도별 3,000회씩 총 9,000회 동안 목표 번째·줄 수·조각 수·정방향과 역방향 답을 독립 검산했고 원본 `7번째→49장`을 재현했다. 브라우저 답안에서 `9을 9번` 같은 숫자 조사 오류를 발견해 조사 없이 읽히는 문장으로 고쳤으며 PC 문제 10개·SVG·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 16번은 원본 문제지·답안지 10쪽의 4x4 번호표, 왼쪽 아래에서 오른쪽 위로 접는 대각선, L자 모양의 두 칸과 대각선 삼각형 한 칸, 답 `18`을 직접 대조했다. 기존 `foldNumberCutSum`은 7세 2차 11번의 다른 칠하기 위치와 답 `39` 구조라 재사용하지 않고 `fold-number-cut-sum-l-shape`와 `foldNumberCutSumLShape`을 분리했다. 같게는 원본과 같은 세 칸, 쉬움은 두 칸, 어렵게는 네 칸을 칠하며 숫자표의 1~8은 각각 두 번씩 무작위 배치한다. 난이도별 3,000회씩 총 9,000회 동안 대각선 반사 좌표·겹치는 칸의 중복 제거·잘린 수·합을 독립 검산했고 원본 잘린 값 `6·4·1·2·5`에서 `18`을 재현했다. 기존 색종이 유형도 3,000회 회귀 검사했다. 브라우저 답안에서 `8를 더하면` 같은 공통 조사 오류를 발견해 두 색종이 생성기의 풀이를 식 형식으로 함께 고쳤으며 PC 문제 10개·SVG·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 17번은 원본 문제지·답안지 11쪽의 네 물건과 세 저울 관계 `○=□+□+◇`, `☆+☆=◇+◇+◇`, `□=☆+◇`, 답 `○=◇ 6개`를 직접 대조했다. 기존 `balanceScaleThreeObjects`는 네 물건을 쓰더라도 원을 네모 몇 개로 바꾸는 다른 관계라 재사용하지 않고 `balance-scale-four-objects`와 `balanceScaleFourObjects`를 분리했다. 쉬움은 별 한 개를 마름모로 바로 바꾸는 도움말, 같게는 원본처럼 별 묶음과 마름모 묶음을 한 번 교환, 어렵게는 네모 안의 별·마름모 수와 교환 횟수를 늘린다. 난이도별 3,000회씩 총 9,000회 동안 세 저울의 양쪽 무게·네모 치환·별 묶음 치환·최종 마름모 수를 독립 검산했고 원본 무게 `☆3·◇2·□5·○12`에서 답 `6개`를 재현했다. 같게 난이도에서 12가지 관계 구성을 확인했으며 PC 문제 10개·관계 그림·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 18번은 원본 문제지·답안지 11쪽의 `☆+☆+☆=○+○+○+○`, `☆+○=▽+▽`, `○+▽=□`, 답 `13`을 직접 대조했다. 기존 `sourceSymbolRelations`는 마지막 식과 난이도별 값이 원본과 달라 재사용하지 않고 `symbolRelationThreeToFour`를 분리했다. 0부터 9까지 서로 다른 세 수를 전수 대입하면 `☆=8, ○=6, ▽=7` 한 가지뿐이다. 첫 두 관계는 모든 난이도에서 원본 그대로 유지하고, 쉬움은 원본 마지막 식과 도움말, 같게는 두 기호의 합, 어렵게는 세 기호의 합으로 목표 식을 조절한다. 난이도별 3,000회씩 총 9,000회 동안 세 관계·서로 다른 한 자리 수·정답을 독립 검산했고, 같게와 어렵게에서 각각 세 가지 정답을 확인했다. PC 문제 10개·기호식·답안·풀이·가로 넘침·콘솔 검사를 통과했다.
- 7세 3차 19번은 원본 문제지·답안지 12쪽의 여섯 점 A~F, 거리 `AD=45·CE=53·AC=26·BD=29·BF=80`, 질문 `EF`, 답 `17`을 직접 대조했다. 7세 2차 19번에서 만든 `numberLineSixPoints`가 같은 다섯 호의 시작점·끝점과 같은 목표 구간을 사용하므로 재사용했다. 단서에서 `CD=AD-AC`, `BC=BD-CD`, `DE=CE-CD`, `EF=BF-BC-CD-DE` 순으로 모든 기본 간격을 독립 복원한다. 쉬움은 BC와 CD 도움말, 같게는 원본과 같은 다섯 겹친 거리, 어렵게는 EF를 구한 뒤 AF까지 구한다. 난이도별 3,000회씩 총 9,000회 동안 복원한 다섯 간격·서로 다른 표시 거리·양수·정답을 검산했고 원본 간격 `16·10·19·34·17`과 답 `17`을 재현했다.

## 다음 순서

1. 7세 3차 20번 `흑백 바둑돌 차로 번째 찾기`를 원본 문제·답안 12쪽과 직접 대조한다. 감사표의 답은 `37번째`이며, 다섯 번째까지 그려진 삼각 배열에서 검은 돌과 흰 돌의 차가 어떤 순서로 늘어나는지 기존 `goStoneDifferenceInverse`의 홀짝 번째 공식과 정확히 같은지 확인한 뒤에만 연다. F01~F23, 7세 2차 20문항, 7세 3차 1~19번은 작업 브랜치에서 완료됐다.
2. 재대조가 끝난 7세 2·3·4·6차의 `생성기 대기` 세부 유형만 원본 그림과 동일한 SVG/HTML 렌더 + 정답 검산기로 구현한다.
3. 각 문항 생성기마다 최소 쉬움·같게·어려움 1,000회 이상 정답·표시값·그림 일치 검산 후 해당 문항만 연다.
4. 파이널 1회의 20유형을 고정 시드로 다시 생성해 중복되지 않는 교체 시험지를 만들고, 문항별 검수 후 프로그램에 연결한다.
5. 초1 선발시험 4종을 같은 방식으로 문제지와 답지부터 분류한다.
6. 1과정 각 권의 본문·단원 테스트를 원본 PPTX 렌더와 답지로 분류하고, 교재 본문 유사문제와 단원 테스트 유사문제를 각각 만든다.

## 확인 명령

```powershell
$node='C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
& $node --check fields-classic/question-bank/source-data.js
& $node --check fields-classic/question-bank/generators.js
& $node --check fields-classic/question-bank/app.js
git diff --check -- fields-classic/question-bank
```
