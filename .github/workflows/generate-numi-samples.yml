name: Generate Numi TTS Samples

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate Numi voice samples
        env:
          GOOGLE_TTS_KEY: ${{ secrets.GOOGLE_TTS_KEY }}
        run: node scripts/generate-numi-samples.js

      - uses: actions/upload-artifact@v4
        with:
          name: numi-tts-samples
          path: numi-samples/*.mp3

      # Actions artifacts download via Azure blob storage, which is unreachable from
      # some sandboxed dev environments. Also push the same files to a disposable
      # branch so they're fetchable with plain `git fetch` instead.
      - name: Push samples to a fetchable branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -B numi-samples-output
          git add -f numi-samples/*.mp3
          git commit -m "Numi TTS samples (auto-generated, temporary)"
          git push -f origin numi-samples-output
