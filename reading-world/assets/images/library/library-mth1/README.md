Drop illustration files here as `page{N}.png`, where N is the on-screen page
number (1-indexed) shown in the reader's page counter — e.g. `page9.png`,
`page27.png`. The reader looks for these two specifically right now (the
book's two illustration-only pages), but any page number works the same way.
No file needed for text pages.
