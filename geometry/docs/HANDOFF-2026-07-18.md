# GFIELD Geometry World 인수인계서

작성일: 2026-07-18

## 작업 위치

- 로컬 작업 폴더: `C:\Users\user\Documents\Codex\2026-07-14\sks\.publish-lete-on`
- GitHub: `https://github.com/docssam1/lete-on`
- 브랜치: `main`
- 서비스: `https://lete-on.gfieldacademy.net/geometry/`

## 이번 작업 완료 내용

- `/geometry/` 접속 시 인트로와 Geometry World 지도로 연결
- 첫 접속 시 이름, 캐릭터, 캐릭터 색상을 반드시 정하는 설정 화면 추가
- 이름과 선택 캐릭터를 `gfield-profile`에 저장하고 재접속 시 유지
- 기존 캐릭터, 포인트, 구매 아이템 데이터를 새 프로필 구조로 이전
- 포인트몰을 모자, 얼굴, 배지, 소품, 효과의 5개 분류와 25개 아이템으로 확장
- 포인트몰 25개 아이템, 분류, 가격, 상단 포인트 표시에 GFIELD SVG 그래픽 적용
- 서로 다른 분류의 아이템을 동시에 착용 가능
- Cube Town 상단에 저장된 사용자 이름과 캐릭터 표시
- 똑같이 쌓기, 쌓기나무 개수 세기, 여러 입체 만들기의 마지막 레벨과 문제 번호 저장
- 모바일 프로필 화면 가로 넘침 수정
- 한국어, 중국어, 일본어, 영어 프로필 문구 적용
- 쌓기나무 개수 세기를 똑같이 쌓기와 같은 원목판, 원목 큐브, 오른쪽 조작 메뉴 디자인으로 통일
- 개수 세기에서 3D 쌓기나무 위 입력과 위에서 본 바탕그림 입력이 같은 답 데이터를 공유하도록 연결
- 입력 위치를 바꿔도 이미 쓴 숫자가 3D 기둥 위와 바탕그림 양쪽에 계속 표시
- 위에서 본 바탕그림을 두 입력 모드에서 항상 보여 주고 모바일 잘림을 수정
- 첫 레벨의 첫 문제 첫 칸만 개념 보기로 제공하고 이후 문제는 빈칸으로 시작
- 덧셈식 영역을 제거하고 전체 개수 입력과 숫자판에 집중하도록 구성

## 저장 구조

브라우저 `localStorage`의 `gfield-profile`에 아래 정보가 저장된다.

- `name`: 사용자가 정한 이름 또는 별명
- `setupComplete`: 첫 설정 완료 여부
- `character`, `color`: 캐릭터와 색상
- `equipped`: 분류별 착용 아이템
- `unlocked`: 구매한 아이템 목록
- `progress`: 게임별 마지막 레벨과 문제 번호

포인트는 기존과 동일하게 `gfield-points`를 사용한다. 현재 저장은 같은 기기와 브라우저에서 이어지는 방식이다. 기기 간 이어하기는 이후 로그인 또는 서버 저장이 필요하다.

## 주요 파일

- `geometry/world-map/index.html`, `app.js`, `styles.css`: 첫 설정, 캐릭터 선택, 포인트몰
- `geometry/shared/profile-storage.js`: 게임 진행 공통 저장
- `geometry/cube-town/`: 게임 목록과 사용자 이름 표시
- `geometry/app.js`: 똑같이 쌓기 진행 저장
- `geometry/games/count-heights/app.js`: 개수 세기 진행 저장
- `geometry/games/shape-build/shape-build.js`: 레벨 4, 5 진행 저장

## 검수 결과

- 첫 접속 설정 화면 표시 확인
- 이름 입력 후 캐릭터와 색상을 바꿔도 입력값 유지 확인
- 저장 후 재접속 시 이름, 캐릭터, 색상 유지 확인
- 모바일 390x844에서 프로필 화면 가로 넘침 없음
- 모바일 가로 844x390에서 세 게임 로딩 및 콘솔 오류 없음
- 로컬 월드맵에서 SVG 포인트몰 아이콘 25종과 아이템/착용/캐릭터 미리보기 렌더링 확인
- 모바일 390x844에서 포인트몰 너비 넘침 없음, 데스크톱에서 5열 아이템과 230px 캐릭터 미리보기 확인
- 모바일 가로 844x390에서 Cube Town 상단 높이를 56px로 압축하고 첫 게임 카드 노출 확인
- 모바일 가로 844x390에서 개수 세기 4x4 바탕그림, 전체 답, 숫자판, 오른쪽 메뉴가 한 화면에 표시됨
- 바탕그림에 입력한 수가 같은 3D 기둥 위에 표시되고 입력 모드 전환 후에도 유지됨
- 데스크톱 1440x900에서 개수 세기 전체 게임 화면이 스크롤 없이 표시됨
- 월드맵 캐시 버전은 `styles.css?v=7`, `app.js?v=7`로 갱신

## 배포와 계정 상태

- 현재 Geometry에는 별도 로그인·백엔드·Geometry 전용 DB가 없다.
- 이름, 캐릭터, 색상, 포인트, 아이템, 게임 진행은 브라우저 `localStorage`에 저장된다.
- 루트 `Dockerfile`은 저장소 전체를 nginx 이미지에 포함하고, 루트 `cloudbuild.yaml`은 Cloud Run 서비스 `hf-viewer` 배포 경로를 정의한다.
- 확인된 `.github/workflows/deploy-pages.yml`은 `reading-world`만 GitHub Pages 대상으로 하므로 Geometry를 Pages 배포 경로로 간주하면 안 된다.
- 기기 간 이어하기를 추가하려면 먼저 기존 계정/DB를 재사용할지, Geometry 전용 인증·저장 API를 만들지 결정해야 한다.

## 다음 우선 작업

1. 학습지의 잘린 쌓기나무 그림 수정, 첫 예제 정답 제공, 인쇄 디자인 통일
2. 개수 세기 첫 문제의 개념 안내를 큐비 튜토리얼과 연결하고 이후 문제에서는 반복 음성을 내지 않도록 정리
3. 큐비 자연 음성을 만화 캐릭터 톤의 다국어 MP3로 교체
4. 사용자 이름과 진행 기록을 서버 계정에 연결해 기기 간 이어하기 지원. 인증/DB 결정 전에는 localStorage 구조를 깨지 않는다.
5. 나머지 Cube Town 게임을 설계 순서대로 구현

## 주의 사항

- 똑같이 쌓기의 기존 드래그 엔진과 빨간 배치 가이드를 유지한다.
- 모바일 게임은 가로 화면 한 장 안에 들어오는 구성을 기준으로 한다.
- 큐비 음성은 튜토리얼 중심으로 제한하고, 일반 배치에서는 짧은 효과음을 사용한다.
- 성공 문구는 `GOOD JOB!`, `GREAT JOB!`, `SUCCESS!` 중 하나를 영어로 표시한다.
