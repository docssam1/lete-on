# 작업 체크포인트 · 2026-09-09

## 범위

이 작업은 `hyper-focus/**`만 수정한다. 공용 루트, 다른 상품, 원본 자료, 실제 학생 기록, DB 및 원격 배포는 변경하지 않는다. 기존 루트 CLI 캐시가 도구 실행으로 갱신된 사실을 보고했고, 사용자가 계속 진행하도록 지시했다. 그 파일은 그대로 보존하고 이후 CLI는 실행하지 않는다.

## 현재 결과와 검증 경계

### 이어서 수행한 작업 (아래 과거 수치보다 우선)

마지막 추가 검수는 새 HF 학생 뷰어249검사 통과다. 324쪽 전부의 텍스트 가로/세로 경계0, 답안 제목을 강제로 이동시킨 잘림 오류 탐지, 복원 후0을 포함한다. 부모가 최종249검사를 다시 실행했다. 실제PDF195쪽의 제목·본문을 원본 해상도 래스터로 다시 확인해 잘림이 없었다. 이하246은 이 세 가지 검사를 추가하기 전 통과 수치다.

HF54의 새 비공개 연습 전달은 로컬 구현과 UI 검증을 마쳤다. 부모 최종 실행: 새 viewer246검사, private server/실제패키지13,049검사, 유형승인283검사, 기존진단5검사, 보호시험UI검사 모두 통과. 실제162풀 대표를 질문·풀이324쪽 PDF로 출력해 넘침0/전체텍스트종이밖0 확인. 아래 `구현 중` 기록은 이 로컬 완료로 대체하되 모든풀held/운영기능false/직접URL분리미완 경계는 유지한다. 교사용 공개 미리보기8837 HTTP200 확인. 공용파일/DB/업로드/배포 변경0이다.

기존 HF54도 사용자 요청에 따라 별도 비공개 연습 전달을 로컬 구현 중이다. 챌린지 104유형과 혼합하지 않는다. `../output/private-practice/latest.json`의 `hf-practice-20260909004831990`은 54유형×3난도, 그림·문장 중복 제거 후 3,169개, 질문/답안 JSON 1,164개다. manifest SHA-256 `ec1ebd00fb2a46172513c2db54d345d3349a3a8e799fa64143524982f46148c3`. 신규 비공개 묶음은 전부 `held`이며 기존 생성기 스모크 통과를 독립 출제 승인으로 바꾸지 않았다. 무료 풀은 그림과 정답이 서로 다른 유형·난도별 고정2개로 따로 보관하고 정답·풀이 문장은 한 번만 표시한다. 일부 유형은 실제로 2개뿐이라 무조건20개를 제공하지 않는다. 기존 고정 유사문항의 draft30개도 변경하지 않았다. 부모가 실제 패키지 독립13,049검사를 재실행해 실패0을 확인했다. 기존 진단5검사(54원문·선택·채점·기록 동일)와 프리미어 보호시험 UI검사도 통과했다. 이전 클라이언트 생성 뷰어18검사는 새 private viewer 검사로 대체하며 통과 수에 중복 합산하지 않는다.

운영 반영을 위한 공용 배포 설정 변경 및 서버/비공개 저장소 적용 범위는 사용자에게 비동기 확인을 요청했다. 답변 전에는 공용 파일·운영 환경을 수정하지 않는다. 기존 HF의 다른 화면에서도 생성기·자료를 사용하므로 새 학생 연습 화면의 요청 분리만으로 기존 직접 URL까지 차단되었다고 주장하지 않는다.

최종 로컬 묶음은 `challenge-20260909002902515`다. manifest SHA-256 `03b492d8e0329b26b5e6478fc9ca33bfba91b28c37158abc0e137f78587894c1`. 문서6/원형104/유형104×3난도, 변형6,439문항, JSON2,014개, 최대5,167,099바이트. 실제 파일/해시/응답 계약 19,187검사, 공개 묶음60검사, 학생UI36검사 모두 통과했다. 실패0이며 원격 변경0이다. 최종 통합 계산18,720+오답18,720 및 실제Studio312문항/12PDF도 재실행 통과. 작업 폴더를 그대로 공개하는 배포는 하지 않는다.

- 원형 104개 모두 쉽게·같게·어렵게 연결. numeric35/geometry25/core 미지원19칸 확장은 각 독립 solver와 부모 통합 18,720문항/18,720오답검사 통과. 다른 에이전트의 별도 검토에서 찾은 0개 점그리기·보이는 주기 답·암호 보기 복사·심화 중간단계 생략 등을 수정하고 재검사했다.
- `challenge-content` 서버, `content-client`, `secure-document`, `remote-variants` 구현. 학생 화면은 공개 메타데이터만 먼저 받으며 원문과 답안은 승인 확인 후 별도 요청한다.
- 분할 비공개 묶음 첫 통과본: `challenge-20260908235959152`, 문서6/원형104/은행104, 268난도 풀, 1,770JSON, 변형5,556개, 최대5,167,099바이트, 독립16,689검사 통과. 최신 묶음은 `../output/private-challenge/latest.json`이 가리킨다. 이는 업로드가 아니라 로컬 산출물이다.
- 실제 공개 묶음에서 직접 원문·답안 경로 거부60검사. 비공개 묶음을 사용한 학생UI36검사(질문 먼저/답안 명시요청/늦은 응답 폐기/다른 버전 답안 차단/유한 풀 수량 제한 포함), 문서 접근30검사, 스튜디오60검사 통과.
- 전체 104×3난도를 실제 스튜디오에서 12개 PDF로 출력, 페이지 넘침0/답안 홀수표지/390px폭/문항과 답안 개수 확인. 마지막 굴림 확대 수정 후 재실행도 통과했다.
- 로컬 8794는 다른 작업 서버와 충돌했다. 다른 작업 프로세스를 보존하고 우리 중복 미리보기 프로세스만 종료한 뒤 8837로 분리했다. 현재 미리보기 `http://127.0.0.1:8837/challenge/?teacherPreview=1`.
- 실제 `hf-challenge-private` 저장소는 읽기 전용 조회에서 존재하지 않았다. 다른 상품 저장소 재사용 없음. 원격 함수배포/저장소생성/업로드/학생승인변경/기존URL차단은 수행하지 않았다.

| 대상 | 구현 및 로컬 근거 | 운영/추가 확인 |
|---|---|---|
| 과정 선택 | 홈 3배너, 챌린지 교재 선택 화면, PC·390px 확인 | 운영 배포 안 함 |
| 개념 | 2권 110문항, 74세부 유형, 49쪽씩, 답안 표지35쪽, 예제 답 없음 | 실제 승인학생별 발급은 운영 연결 후 |
| 모의고사 | 4회 본시험80+추가24, 회당21쪽, 답안 표지15쪽, PDF 응답선 래스터 검사 | 난이도 비율은 편집 목표, 통계 보정 아님 |
| 진단 | 336검사, 60점, 미입력 분리, 영역/소영역/유형 근거, 오답만 선택 | 실제 성적 저장 안 함 |
| 챌린지 승인 관리 | 110키, 463로컬 검사, 자동 갱신 및 정상 토큰 갱신 시 화면 유지 | 실제 RLS/세션/학생간 거부는 운영에서 별도 확인 |
| 기존 HF 유형 승인 | 54유형+전환키, 별도 관리자, 서비스283검사와 직접URL/준비중 만료/학생 변경 등 독립23검사 | 신규 함수/마이그레이션 선행 적용 필요. 프리미어/VIP 변경 없음 |
| 교재 접근 | 독립30검사+실제 패키지 UI36검사, 미승인 콘텐츠 JS요청0, 만료/학생명 제거, 인쇄직전 확인 | 운영 호스팅 설정 변경 및 실계정 검증 필요 |
| 문제은행 | 원형104 미리보기, 104×3난도 지원, 독립18720+음성18720 검사 | 실제 성적에 따른 난도 보정은 아님 |
| 스튜디오 | 독립60검사, 오답 선택/실제 문제 미리보기/학생별 고정 학습지/4종 인쇄 | 실제 성적 저장이나 운영 발급 아님 |

출시 게이트는 `ACCESS_CONTRACT.md`가 우선한다. UI 권한검사나 모의 서버 테스트를 실제 비공개 제공 성공으로 해석하지 않는다. `CHALLENGE_PRIVATE_DELIVERY_READY`는 확인 전 false 유지.

## 후속 검수

1. 최신 변형·스튜디오·PDF와 기존 프리미어 UI 회귀 검사를 부모가 재실행했다. 실제 학생·원격 쓰기는 사용하지 않았다.
2. 기존 HF54유형의 신규 서비스 최종 QA 수는 `../output/qa/hyperfocus-type-access/report.json`을 확인한다. 경계23검사와 기존 진단 선택/채점/기록 원본 대조를 통과했다.
3. 비공개 전달과 분리된 공개 묶음은 구현·로컬 검수했다. 운영 적용과 기존 공개 URL 거부는 남아 있다. 준비 플래그를 켜서 이 검증을 우회하지 않는다. 저장소의 공용 Pages workflow가 HF 폴더 전체를 복사하며 Docker/nginx도 공개 웹 루트로 포함하는 구조이므로 사용자 승인 전 공용 파일을 수정하거나 배포하면 안 된다.
4. 백엔드 적용·실제 인증 검증을 먼저 수행하기 전에는 신규 HF 유형 승인을 요구하는 화면을 운영에 배포하지 않는다. 프런트엔드만 배포하면 기존 학생의 유사문제 이용이 막힐 수 있다.

현재 결과물은 로컬 교사 검토본이며, 원격 공개 또는 학생 발급 완료가 아니다.

## 2026-09-09 후속 작업: 운영 준비와 상세 풀이 재검수

- 공용 배포 설정 및 운영 서버 적용 승인 후, 지정된 HF 서버에 challenge-access, challenge-content, hyperfocus-type-access, hyperfocus-practice-content 함수 v1을 배포했다. 승인 항목은 Challenge 110개와 HF 유형 55개를 등록했다. 기존 학생 승인·기록은 변경하지 않았다. 비공개 자료 업로드 및 실제 학생 발급 검증은 아직 끝나지 않았다.
- 공개 GitHub 저장소에는 원본 작업 브랜치의 교사용 문항·정답·그림·PDF 이력을 올리지 않는다. 최신 main에서 별도 release 작업트리를 만들고 공개 화면 허용 목록만 선별한다. 다른 제품과 기존 Pages 방식을 임의 변경하지 않는다.
- 사용자 추가 요청에 따라 3D 책 서재와 단독 소개 페이지를 구현 중이다. 상담 주소는 기존 portal-data.js의 https://open.kakao.com/me/gfield 를 재사용한다.
- 상세 풀이 재감사: 기존 패키지의 빈 풀이·미정 표시 0건은 확인했으나, 이를 상세 풀이 완성으로 해석하지 않는다. 기호식 유사문제 144건은 풀이가 정답식과 같고, 순서 유형 288건은 조건별 도출 과정이 부족하여 보완 중이다. 이전 패키지를 이 상태로 업로드하지 않는다.
- 원형 3회 1번·15번, 4회 17번의 각 빈칸 계산과 비교 과정을 보완했다. 같은 원형을 쓰는 개념의 빈칸 비교 예제·유제에도 연결된다. 수정 후 원형 52문항 및 개념 110문항 독립 검산 통과. 모의 4회 A4/390px, 개념 2권 A4/390px 렌더 재실행에서 넘침·정답 시작 면 오류 0건이다.
- 다음 단계: 유사문제 상세 풀이 보완 검수 → 최종 자료 재생성/해시 검증 → 비공개 업로드 → 공개 UI 배포 → 실제 접근 검증. 공개 UI 또는 모의 서버 테스트만으로 학생 발급 완료를 주장하지 않는다.
