# Hyper Focus 단일정답·가시성 결정

검수일: 2026-08-20

## 시험 구성과 문제은행

- 모의고사의 문항 수·순서·난이도는 생성기 기본값이 아니라 이름이 있는 시험 구성표에서 결정한다.
- 현재 `spatial-generator-review`는 q01~q05 연결 검수용이다. 정식 프리미어 모의고사로 표시하지 않는다.
- 약점 문제은행의 유형별 문제 수는 사용자가 직접 입력한다. 임의의 기본 문항 수를 두지 않는다.

## q02 상자 채우기

- 낮은 등각 시점에서 앞 기둥이 뒤 기둥의 높이를 가리는 그림은 출제하지 않는다.
- 유형 2는 윗면이 넓게 보이는 `elevated-open-box` 시점을 사용하고 가로·세로·높이 칸 수를 문장과 그림 아래에 함께 제공한다.
- 기둥 높이는 상자의 먼 왼쪽 모서리에서 보는 쪽으로 갈수록 같거나 낮아야 한다. 두 바닥 방향 중 하나라도 다시 높아져 뒤 기둥을 가리면 생성 단계에서 탈락시킨다.
- 생성 후 상자 전체 수, 현재 쌓인 수, 더 필요한 수를 다시 계산하고 정답 후보가 1개일 때만 통과시킨다.

## q03 흑백 교차 쌓기나무

- 전체 흰색·검은색 수를 물을 때는 각 바닥 칸의 정확한 높이와 한 기준 칸의 시작 색을 함께 제공한다.
- `어렵게`의 층수가 제각각인 모양은 낮은 시점으로 그리지 않고, 모든 바닥 위치의 윗면이 드러나는 `elevated-color-stack` 시점으로 그린다.
- 높은 시점의 3D 그림은 색 배열을 읽는 보조 그림이고, 각 기둥의 정확한 높이는 함께 제공하는 위에서 본 수 바탕그림으로 확정한다.
- 생성 후 높이표와 기준 색에서 가능한 정답 후보를 다시 계산하고 후보가 1개일 때만 통과시킨다.

## q04 구멍 뚫기

- 바깥 테두리에 걸친 구멍은 출제하지 않고 모든 터널 입구를 각 면의 안쪽 칸에 둔다.
- `쉽게`는 한 방향의 구멍 1개로 겹침 계산 없이 푼다.
- `같게`는 두 방향의 구멍 4~5개로 만들고 서로 교차하는 칸이 2곳 이상일 때만 통과시킨다.
- `어렵게`는 세 방향의 구멍 6~8개로 만들고 세 구멍이 한 칸에서 만나는 교차를 반드시 포함한다.
- 교차 칸은 여러 번 빼지 않고 한 번만 빼며, 층별 남은 수의 합과 좌표 전수계산이 같아야 한다.

## q05 보이지 않는 쌓기나무

- 각 바닥 칸의 정확한 높이, 벽 유무, 관찰 방향을 문제 문장과 그림에 함께 제공한다.
- 벽 있음은 위·앞·오른쪽, 벽 없음은 위·앞·뒤·왼쪽·오른쪽에서 모두 가려진 경우로 구분한다.
- 벽 없음의 숨은 수는 `쉽게` 1~2개, `같게` 5~9개, `어렵게` 10~20개로 분리한다.
- 벽 있음의 숨은 수는 `쉽게` 3~4개, `같게` 5~9개, `어렵게` 10~20개로 분리한다.
- 벽 유무가 달라도 전체 범위가 `쉽게` 1~4개, `같게` 5~9개, `어렵게` 10~20개로 겹치지 않게 한다.
- 생성 후 조건 전체에서 가능한 정답 후보를 다시 계산하고 후보가 1개일 때만 통과시킨다.

## 모바일 뷰어

- 상단 도구막대는 모바일에서도 한 줄을 유지한다.
- 버튼 폭이 화면보다 넓으면 내용 위에 여러 줄로 겹치지 않고 가로 스크롤한다.

## 프리미어 원본 모의고사

- 영상 대본과 PDF 뒤쪽 풀이는 원본 문항을 찾는 힌트일 뿐 정답 근거로 확정하지 않는다. 문제 번호·구조가 대응하는지 먼저 대조하고, 대응하지 않으면 문항별로 다시 계산한다.
- 문제 문장과 그림의 개수·방향·조건이 충돌하면 계산 가능한 답이 있더라도 공개하지 않는다. 수정 내용을 기록하고 승인한 뒤 다시 검산한다.
- 회전과 뒤집기를 가르는 문항은 축소 미리보기로 판정하지 않는다. 원본을 충분한 해상도로 확인하고 접합점 주변 특징의 순환 순서가 보존되는지 비교한다.
- 그리기·배치형 문항은 여러 완성 그림이 가능한지 먼저 열거한다. 여러 답을 허용하는 원본이면 자동 단답 채점으로 바꾸지 말고 허용 답 계약이나 자기 채점 방식을 별도로 둔다.
- 프리미어 고유 유형을 이름이 비슷하다는 이유만으로 Hyper Focus q01~q54에 연결하지 않는다. 구조·조건·질문 방식이 모두 같을 때만 정확 일치로 연결한다.
- 시험지는 HTML로 다시 그리지 않고 원본 PDF의 문제 쪽만 페이지 이미지로 분할해 필즈더클래식 인쇄 뷰어에서 보여 준다. 관련 없는 해설·빈 쪽은 포함하지 않는다.
- O/X 진단은 원본 이미지 자체가 단일정답·가시성 검수를 통과한 문항만 분모에 넣는다. 대체 HTML 문항만 검수된 경우 원본 이미지 문항은 계속 `채점 제외`로 둔다.
- 진단 화면에는 영역·유형·검수 상태만 내보내고 정답, 원본 위치, 비공개 감사 요약은 넣지 않는다.
- 저장소가 공개 상태인 동안 유료 원본의 페이지 이미지 파일은 GitHub에 올리거나 배포하지 않는다. 비공개 저장소 또는 접근 제어 저장소로 전환한 뒤에만 자산을 연결한다.

## 검증 위치

- 시험 구성표: `hyper-focus/mock/exam-blueprints.js`
- q02~q05 생성·가시성·난이도·후보 검사: `hyper-focus/generator/stacking.js`
- 런타임 계약: `hyper-focus/mock/mock-core.js`
- 전수·회귀검사: `hyper-focus/qa/validate_mock.js`
- q04~q09 렌더·문장·난이도 분리 추가 검사: `hyper-focus/qa/verify_q04_q09_visual_contract.js`
- 프리미어 이미지 페이지 검사: `hyper-focus/qa/validate_premier_page_images.cjs`
- 프리미어 채점 가능 수·영역·유형 진단 검사: `hyper-focus/qa/validate_premier_diagnosis.cjs`
