{
  "name": "gfield-highschool-selection",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node --test tests/exam-editor-browser.test.cjs",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
