server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
    location ~* \.(json|js|css|html|png|jpg|svg)$ {
        expires 1h;
        add_header Cache-Control "public";
    }
}
