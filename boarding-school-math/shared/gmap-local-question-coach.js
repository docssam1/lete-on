(function () {
  "use strict";

  var config = window.GMAPQuestionCoachConfig || {};
  var CARD_SELECTOR = config.cardSelector || ".problem-card,.book-problem,.question-paper,.lesson-paper";
  var REMOVE_SELECTOR = ".teacher-solution,.teacher-key,.teacher-move,.teacher-answer,.solution-box,.answer-row,.response-row,.feedback,.choice-feedback,.gmap-ai-launch,.gmap-ai-dialog,script,style";
  var activeCard = null;

  var COPY = {
    ko: {
      launch: "문제 질문 도우미", title: "이 문제, 어디서 막혔나요?", context: "지금 보고 있는 문제", intro: "검수된 문제은행과 현재 풀이 상태를 바탕으로 다음 단계를 안내합니다.",
      hint: "힌트 한 단계", why: "풀이 방향", analyze: "현재 풀이 분석", similar: "유사문제 찾기", placeholder: "궁금한 점이나 지금까지 푼 과정을 적어 주세요.", send: "질문하기", close: "닫기", student: "학생", assistant: "질문 도우미",
      note: "외부 AI나 API를 호출하지 않습니다. 정답과 채점은 검수된 문제은행을 기준으로 합니다.", empty: "질문을 입력해 주세요.", hintGeneric: "문제에서 주어진 값과 구하려는 값을 각각 표시한 뒤, 둘을 연결하는 관계를 한 줄로 적어 보세요.",
      whyGeneric: "{type} 유형입니다. 조건을 작은 관계로 정리하고, 각 관계가 모두 맞는지 확인한 다음 계산하세요.", noneAnalysis: "아직 확인된 풀이 기록이 없습니다. 먼저 답을 선택하거나 입력한 뒤 다시 분석을 눌러 보세요.", wrongAnalysis: "현재 답은 다시 확인해야 합니다. 아래 힌트로 사용한 조건과 계산 순서를 점검하세요.", correctAnalysis: "현재 답은 검수된 판정과 일치합니다. 이제 사용한 관계와 다른 선택지가 성립하지 않는 이유를 말로 설명해 보세요.",
      conceptAnalysis: "이 화면은 개념 학습입니다. 핵심 표현과 완전 풀이 예제를 연결하고 자주 하는 오해와 비교해 보세요.", watchFor: "확인할 오류", teacherAnalysis: "교사용 검수 해설", similarLead: "같은 학습 묶음에서 이어서 풀 수 있는 검수 문항입니다.", openSimilar: "이 문제로 이동", answerGuard: "학생용에서는 정답을 바로 제시하지 않습니다. 먼저 시도한 뒤 ‘현재 풀이 분석’을 눌러 확인하세요.", teacherAnswer: "교사용 화면의 검수된 답과 풀이입니다.",
      presets: ["힌트를 한 단계만 보여 주세요.", "이 문제의 풀이 방향을 알려 주세요.", "현재 풀이를 분석해 주세요.", "같은 영역의 유사문제를 찾아 주세요."]
    },
    en: {
      launch: "Question coach", title: "Where did this problem become difficult?", context: "Current problem", intro: "Guidance comes from the verified question bank and your current attempt.",
      hint: "One-step hint", why: "Method direction", analyze: "Analyze my attempt", similar: "Find similar practice", placeholder: "Ask a question or describe what you have tried.", send: "Ask", close: "Close", student: "Student", assistant: "Question coach",
      note: "No external AI or API is called. Final answers and grading use the verified question bank.", empty: "Enter a question first.", hintGeneric: "Mark what is given and what you need to find, then write one relationship connecting them.",
      whyGeneric: "This is a {type} problem. Break the conditions into small relationships, verify each one, and then calculate.", noneAnalysis: "There is no checked attempt yet. Choose or enter an answer, then analyze it again.", wrongAnalysis: "This response needs another check. Use the hint below to review the conditions and calculation order.", correctAnalysis: "This response matches the verified result. Now explain the relationship you used and why the other choices do not work.",
      conceptAnalysis: "This is a concept lesson. Connect the central representation to the worked example and compare it with the common misconception.", watchFor: "Watch for", teacherAnalysis: "Verified teacher explanation", similarLead: "Here is another verified item from the same learning group.", openSimilar: "Go to this problem", answerGuard: "The student view does not reveal the answer immediately. Try the problem first, then use attempt analysis.", teacherAnswer: "Here is the verified answer and explanation from the teacher view.",
      presets: ["Show only one hint.", "Show the method direction for this problem.", "Analyze my current attempt.", "Find a similar problem in this learning group."]
    },
    "zh-Hans": {
      launch: "题目辅导", title: "这道题卡在哪里？", context: "当前题目", intro: "根据已审核题库和当前作答状态，提示下一步。",
      hint: "给一步提示", why: "解题方向", analyze: "分析当前作答", similar: "查找同类题", placeholder: "写下你的问题，或说明你已经做到哪一步。", send: "提问", close: "关闭", student: "学生", assistant: "题目辅导",
      note: "不调用外部AI或API。答案和评分以已审核题库为准。", empty: "请先输入问题。", hintGeneric: "先分别标出已知量和要求的量，再写出连接它们的一条关系。",
      whyGeneric: "这是{type}题。先把条件整理成几个小关系，逐一核对后再计算。", noneAnalysis: "目前还没有已检查的作答。请先选择或输入答案，再进行分析。", wrongAnalysis: "当前答案需要重新检查。请根据下面的提示核对条件和计算顺序。", correctAnalysis: "当前答案与审核结果一致。请继续说明所用关系，以及其他选项为什么不成立。",
      conceptAnalysis: "这是概念学习页面。请把核心表示与完整例题联系起来，并与常见误区进行比较。", watchFor: "注意错误", teacherAnalysis: "教师版审核解析", similarLead: "这是同一学习组中的另一道已审核题目。", openSimilar: "前往这道题", answerGuard: "学生版不会立即显示答案。请先尝试作答，再使用作答分析。", teacherAnswer: "以下是教师版中的已审核答案和解析。",
      presets: ["只给我一步提示。", "告诉我这道题的解题方向。", "分析我当前的作答。", "在同一学习组中查找同类题。"]
    }
  };

  function locale() { var lang = document.documentElement.lang || "ko"; return lang.toLowerCase().startsWith("zh") ? "zh-Hans" : lang.toLowerCase().startsWith("en") ? "en" : "ko"; }
  function copy() { return COPY[locale()] || COPY.ko; }
  function audience() { if (document.body.dataset.audience) return document.body.dataset.audience; var selected = document.querySelector('[data-audience][aria-selected="true"],[data-audience][aria-pressed="true"]'); return selected && selected.dataset.audience === "teacher" ? "teacher" : "student"; }
  function compact(value, limit) { return String(value || "").replace(/\s+/g, " ").trim().slice(0, limit); }
  function isVisible(node) { return node && !node.hidden && getComputedStyle(node).display !== "none"; }
  function custom(name, fallback) { if (typeof config[name] !== "function") return fallback; try { return config[name].apply(null, Array.prototype.slice.call(arguments, 2)); } catch (_error) { return fallback; } }
  function context(card) { var clone = card.cloneNode(true); clone.querySelectorAll(REMOVE_SELECTOR).forEach(function (node) { node.remove(); }); var title = clone.querySelector("h1,h2,h3,h4,.question-number,.problem-meta"); var prompt = clone.querySelector(".problem-prompt,.prompt-text,.problem-question,.question-card,.lesson-copy"); return { id: card.dataset.itemId || card.id || "current-problem", title: compact(title && title.textContent, 240), prompt: compact(prompt && prompt.textContent, 1600) || compact(clone.textContent, 2600) }; }
  function contextLabel(value) { return compact([value.title, value.prompt].filter(Boolean).join(" · "), 210); }
  function icon() { return '<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 5.5h14v10H9l-4 3v-13Z"/><path d="M9 9h6M9 12h4"/></svg>'; }

  function createDialog() {
    var node = document.createElement("dialog"); node.className = "gmap-ai-dialog"; node.id = "gmap-ai-dialog"; node.setAttribute("aria-labelledby", "gmap-ai-title");
    node.innerHTML = '<div class="gmap-ai-shell"><header class="gmap-ai-header"><div><p class="gmap-ai-kicker">G·MAP QUESTION COACH</p><h2 class="gmap-ai-title" id="gmap-ai-title"></h2></div><button class="gmap-ai-close" type="button">×</button></header><section class="gmap-ai-context"><strong></strong><p></p></section><div class="gmap-ai-log" role="log" aria-live="polite"></div><section class="gmap-ai-controls"><div class="gmap-ai-presets"></div><form class="gmap-ai-form"><input class="gmap-ai-input" type="text" autocomplete="off" spellcheck="false"><button class="gmap-ai-send" type="submit"></button></form><p class="gmap-ai-status" role="status" aria-live="polite"></p><p class="gmap-ai-note"></p></section></div>';
    document.body.append(node); node.querySelector(".gmap-ai-close").addEventListener("click", function () { node.close(); }); node.addEventListener("click", function (event) { if (event.target === node) node.close(); });
    node.querySelector("form").addEventListener("submit", function (event) { event.preventDefault(); var input = node.querySelector(".gmap-ai-input"); var value = input.value.trim(); if (!value) { node.querySelector(".gmap-ai-status").textContent = copy().empty; input.focus(); return; } input.value = ""; respond(value); });
    return node;
  }
  function dialog() { return document.getElementById("gmap-ai-dialog") || createDialog(); }
  function appendMessage(role, message) { var c = copy(); var wrap = document.createElement("div"); wrap.className = "gmap-ai-message"; wrap.dataset.role = role; var label = document.createElement("small"); label.textContent = role === "user" ? c.student : c.assistant; var content = document.createElement("p"); content.textContent = message; wrap.append(label, content); var log = dialog().querySelector(".gmap-ai-log"); log.append(wrap); log.scrollTop = log.scrollHeight; return wrap; }
  function appendTarget(message, target) { var wrap = appendMessage("assistant", message); var button = document.createElement("button"); button.type = "button"; button.className = "gmap-ai-message-action"; button.textContent = copy().openSimilar; button.addEventListener("click", function () { dialog().close(); target.setAttribute("tabindex", "-1"); target.focus({ preventScroll: true }); target.scrollIntoView({ behavior: "smooth", block: "center" }); target.classList.add("gmap-ai-target"); window.setTimeout(function () { target.classList.remove("gmap-ai-target"); }, 1800); }); wrap.append(button); }
  function updateDialog(value) { var c = copy(); var node = dialog(); node.querySelector(".gmap-ai-title").textContent = c.title; node.querySelector(".gmap-ai-close").setAttribute("aria-label", c.close); node.querySelector(".gmap-ai-context strong").textContent = c.context; node.querySelector(".gmap-ai-context p").textContent = contextLabel(value); node.querySelector(".gmap-ai-input").placeholder = c.placeholder; node.querySelector(".gmap-ai-send").textContent = c.send; node.querySelector(".gmap-ai-note").textContent = c.note; node.querySelector(".gmap-ai-status").textContent = ""; var presets = node.querySelector(".gmap-ai-presets"); presets.replaceChildren(); [c.hint, c.why, c.analyze, c.similar].forEach(function (label, index) { var button = document.createElement("button"); button.type = "button"; button.className = "gmap-ai-preset"; button.textContent = label; button.addEventListener("click", function () { respond(c.presets[index]); }); presets.append(button); }); node.querySelector(".gmap-ai-log").replaceChildren(); appendMessage("assistant", c.intro); }
  function typeOf(card) { return compact((card.querySelector("h3,h4,.problem-meta span:last-child,.practice-heading h2") || {}).textContent, 100) || copy().context; }
  function hintFor(card) { var node = card.querySelector(".hint-box,.teacher-move,.choice-feedback.wrong"); var fallback = compact(card.dataset.coachHint || (node && node.textContent), 520) || copy().hintGeneric; return compact(custom("getHint", fallback, card, locale()), 520) || fallback; }
  function misconceptionFor(card) { var node = card.querySelector(".misconception-card p,.teacher-solution small,.teacher-answer:nth-of-type(2)"); var fallback = compact(card.dataset.coachMisconception || (node && node.textContent), 520); return compact(custom("getMisconception", fallback, card, locale()), 520) || fallback; }
  function attemptFor(card) { var fallback = card.querySelector(".feedback.correct,.feedback.is-correct,.choice-feedback.correct") || card.classList.contains("is-correct") ? "correct" : card.querySelector(".feedback.wrong,.feedback.is-wrong,.choice-feedback.wrong") ? "wrong" : "none"; var state = custom("getAttemptState", fallback, card); return ["correct", "wrong", "none"].includes(state) ? state : fallback; }
  function analysisFor(card) { var c = copy(); if (card.matches(".lesson-paper")) { var conceptMistake = misconceptionFor(card); return c.conceptAnalysis + (conceptMistake ? "\n\n" + c.watchFor + " · " + conceptMistake : ""); } if (audience() === "teacher") { var nodes = Array.from(card.querySelectorAll(".teacher-solution,.teacher-key,.teacher-answer")).filter(isVisible); var teacherText = compact(nodes.map(function (node) { return node.textContent; }).join("\n"), 1500); return c.teacherAnalysis + "\n\n" + (teacherText || hintFor(card)); } var state = attemptFor(card); var lead = state === "correct" ? c.correctAnalysis : state === "wrong" ? c.wrongAnalysis : c.noneAnalysis; var detail = state === "correct" ? "" : "\n\n" + c.hint + " · " + hintFor(card); var mistake = misconceptionFor(card); if (mistake) detail += "\n\n" + c.watchFor + " · " + mistake; return lead + detail; }
  function cards() { return Array.from(document.querySelectorAll(CARD_SELECTOR)).filter(isVisible); }
  function similarTo(card) { var all = cards(); var selected = custom("findSimilar", null, card, all); if (selected && selected !== card && all.includes(selected)) return selected; var groupId = card.dataset.coachGroup; if (groupId) { var grouped = all.find(function (node) { return node !== card && node.dataset.coachGroup === groupId; }); if (grouped) return grouped; } var group = card.closest(".problem-page,.book-page,.practice-section,.problem-row"); var nearby = group ? Array.from(group.querySelectorAll(CARD_SELECTOR)).filter(function (node) { return node !== card && isVisible(node); }) : []; if (nearby.length) return nearby[0]; var index = all.indexOf(card); return all[index + 1] || all.find(function (node) { return node !== card; }) || null; }
  function classify(query) { var value = query.toLowerCase(); if (/유사|비슷|\bsimilar\b|\banother\b|同类|相似/.test(value)) return "similar"; if (/분석|시도|풀이.*확인|\banaly\w*\b|\battempt\b|分析|作答/.test(value)) return "analyze"; if (/정답|답은|\banswer\b|答案/.test(value)) return "answer"; if (/왜|방법|방향|\bhow\b|\bwhy\b|\bmethod\b|为什么|方向|方法/.test(value)) return "why"; if (/실수|오류|\bmistake\b|\berror\b|错误|误区/.test(value)) return "mistake"; return "hint"; }
  function respond(query) { if (!activeCard) return; var c = copy(); var mode = classify(query); appendMessage("user", query); if (mode === "similar") { var target = similarTo(activeCard); if (!target) { appendMessage("assistant", c.hintGeneric); return; } appendTarget(c.similarLead + "\n\n" + contextLabel(context(target)), target); return; } if (mode === "analyze") { appendMessage("assistant", analysisFor(activeCard)); return; } if (mode === "answer") { appendMessage("assistant", audience() === "teacher" ? c.teacherAnswer + "\n\n" + analysisFor(activeCard) : c.answerGuard); return; } if (mode === "mistake") { var mistake = misconceptionFor(activeCard); appendMessage("assistant", mistake ? c.watchFor + " · " + mistake : analysisFor(activeCard)); return; } if (mode === "why") { appendMessage("assistant", c.whyGeneric.replace("{type}", typeOf(activeCard))); return; } appendMessage("assistant", hintFor(activeCard)); }
  function openFor(card) { activeCard = card; updateDialog(context(card)); var node = dialog(); if (typeof node.showModal === "function") node.showModal(); else node.setAttribute("open", ""); node.querySelector(".gmap-ai-input").focus(); }
  function attach(card) { if (!(card instanceof HTMLElement) || card.dataset.questionCoach === "ready" || card.hidden || card.closest("[hidden]")) return; card.dataset.questionCoach = "ready"; var button = document.createElement("button"); button.type = "button"; button.className = "gmap-ai-launch"; button.innerHTML = icon() + "<span></span>"; button.querySelector("span").textContent = copy().launch; button.addEventListener("click", function () { openFor(card); }); card.append(button); }
  function scan(root) { if (root instanceof HTMLElement && root.matches(CARD_SELECTOR)) attach(root); (root || document).querySelectorAll(CARD_SELECTOR).forEach(attach); }
  function refresh() { var label = copy().launch; document.querySelectorAll(".gmap-ai-launch span").forEach(function (node) { if (node.textContent !== label) node.textContent = label; }); }
  scan(document); new MutationObserver(function (records) { records.forEach(function (record) { record.addedNodes.forEach(function (node) { if (node.nodeType === 1) scan(node); }); }); refresh(); }).observe(document.body, { childList: true, subtree: true });
})();
